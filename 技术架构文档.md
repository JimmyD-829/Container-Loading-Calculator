# 物流集装箱装柜测算系统 — 技术架构文档

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档名称 | 技术架构文档 |
| 文档版本 | V2.0 |
| 编写日期 | 2026年5月24日 |
| 当前版本 | V1.6 |
| 目标读者 | 技术团队、架构师、开发人员 |

---

## 一、架构概述

### 1.1 系统定位

本系统是一个智能3D装载规划与可视化系统，旨在帮助物流企业优化集装箱装载方案，提高空间利用率，降低运输成本。

### 1.2 架构原则

| 原则 | 说明 |
|------|------|
| **模块化** | 功能解耦，独立演进 |
| **可扩展性** | 支持新增算法、约束类型 |
| **高性能** | 算法优化、并行计算 |
| **可视化** | 3D实时渲染、交互反馈 |
| **易用性** | 直观的操作界面 |

### 1.3 架构风格

- **前端**: 组件化单页应用 (SPA)
- **算法**: 插件式算法框架，支持动态切换
- **数据**: 状态管理中心化

---

## 二、技术栈

### 2.1 前端技术栈

| 分类 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 框架 | React | 18.x | UI框架 |
| 语言 | TypeScript | 5.x | 类型安全 |
| 构建 | Vite | 8.x | 构建工具 |
| UI组件 | Ant Design | 5.x | 组件库 |
| 3D渲染 | Three.js | 0.160+ | 3D可视化 |
| 3D封装 | @react-three/fiber | 8.x | React Three.js封装 |
| 3D助手 | @react-three/drei | 9.x | Three.js工具组件 |
| 状态管理 | Zustand | 4.x | 轻量级状态管理 |
| 图标 | @ant-design/icons | 5.x | 图标库 |
| 表格 | rc-table | 7.x | 表格组件 |

### 2.2 算法技术栈

| 分类 | 技术 | 用途 |
|------|------|------|
| 装箱算法 | FFD/BFD | 基础装箱算法 |
| 智能优化 | GA遗传算法 | 全局优化 |
| 智能优化 | SA模拟退火 | 局部搜索优化 |
| 多目标优化 | 加权评分体系 | 多维度优化 |
| 多柜优化 | 组合优化算法 | 多箱型联合计算 |
| 约束求解 | 自定义约束引擎 | 多约束验证 |

### 2.3 开发工具

| 工具 | 用途 |
|------|------|
| ESLint | 代码规范检查 |
| Prettier | 代码格式化 |
| Jest | 单元测试 |
| GitHub Actions | CI/CD |
| Cloudflare Pages | 部署平台 |

---

## 三、系统架构

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        前端展示层                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │ 货物管理 │  │ 集装箱   │  │ 计算配置 │  │    3D可视化      │   │
│  │          │  │   选择   │  │          │  │                  │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────────┘   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                         │
│  │ 模板管理 │  │ 报表导出 │  │ 历史方案 │                         │
│  └──────────┘  └──────────┘  └──────────┘                         │
└───────────────────────────┬────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        业务逻辑层                                  │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐  │
│  │   状态管理        │  │   算法调度器      │  │   约束验证器      │  │
│  │   Zustand        │  │   Algorithm      │  │   Constraint     │  │
│  │                  │  │   Scheduler      │  │   Validator      │  │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘  │
│  ┌──────────────────┐  ┌──────────────────┐                        │
│  │   模板管理器      │  │   导出处理器      │                        │
│  │   Template       │  │   Export         │                        │
│  │   Manager        │  │   Processor      │                        │
│  └──────────────────┘  └──────────────────┘                        │
└───────────────────────────┬────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        算法引擎层                                  │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐   │
│  │   FFD      │  │   BFD      │  │    GA      │  │    SA      │   │
│  │ 装箱算法   │  │ 装箱算法   │  │ 遗传算法   │  │ 模拟退火   │   │
│  └────────────┘  └────────────┘  └────────────┘  └────────────┘   │
│  ┌────────────┐  ┌────────────┐  ┌────────────────────────────┐   │
│  │ 多目标优化 │  │ 多柜优化   │  │       约束求解器            │   │
│  └────────────┘  └────────────┘  └────────────────────────────┘   │
└───────────────────────────┬────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        数据模型层                                  │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐   │
│  │   Cargo    │  │Container  │  │  Packing   │  │  Result    │   │
│  │   货物     │  │   集装箱   │  │   装载     │  │   结果     │   │
│  └────────────┘  └────────────┘  └────────────┘  └────────────┘   │
│  ┌────────────┐  ┌────────────┐                                   │
│  │ Template   │  │  Export    │                                   │
│  │   模板     │  │  导出配置   │                                   │
│  └────────────┘  └────────────┘                                   │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 模块划分

| 模块 | 职责 | 关键文件 |
|------|------|----------|
| **components** | UI组件层 | `src/components/` |
| **algorithms** | 算法引擎 | `src/algorithms/` |
| **store** | 状态管理 | `src/store/` |
| **types** | 类型定义 | `src/types/` |
| **data** | 模拟数据 | `src/data/` |
| **utils** | 工具函数 | `src/utils/` |

---

## 四、核心模块设计

### 4.1 组件层结构

```
src/components/
├── App.tsx              # 主应用组件
├── CargoManager.tsx     # 货物管理组件
├── ContainerSelector.tsx # 集装箱选择组件
├── CalculationConfig.tsx # 计算配置组件
├── ResultViewer.tsx     # 结果查看组件
├── Container3D.tsx      # 3D可视化组件
├── CenterOfGravity.tsx  # 重心可视化组件
├── StepIndicator.tsx    # 步骤指示器
├── DataTable.tsx        # 数据表格组件
├── AlgorithmSelector.tsx # 算法选择组件
├── ExportPanel.tsx      # 导出面板组件
└── TemplateManager.tsx  # 模板管理组件
```

### 4.2 算法层结构

```
src/algorithms/
├── ffd.ts               # FFD装箱算法
├── constraintSolver.ts  # 约束求解器
├── ga.ts                # GA遗传算法
├── sa.ts                # SA模拟退火
├── multiObjective.ts    # 多目标优化
├── multiContainer.ts    # 多柜联合优化
└── index.ts             # 算法导出入口
```

### 4.3 工具层结构

```
src/utils/
├── exportUtils.ts       # 导出工具函数
├── templateManager.ts   # 模板管理器
├── storageUtils.ts      # 本地存储工具
└── validationUtils.ts   # 验证工具函数
```

### 4.4 状态管理层

```typescript
// store/index.ts 状态结构
interface AppState {
  // 货物管理
  cargo: {
    cargos: Cargo[];
    selectedCargoIds: string[];
  };
  
  // 集装箱选择
  containers: {
    selectedContainers: { spec: ContainerSpec; quantity: number }[];
    customContainers: ContainerSpec[];
  };
  
  // 计算设置
  settings: {
    algorithm: 'FFD' | 'BF' | 'GA' | 'SA' | 'MultiObjective';
    allowRotation: boolean;
    prioritizeWeight: boolean;
    maxIterations: number;
    timeLimit: number;
    weights: {
      utilization: number;
      balance: number;
      stacking: number;
      loading: number;
    };
  };
  
  // 计算结果
  result: PackingResult | null;
  
  // UI状态
  isCalculating: boolean;
  error: string | null;
  
  // 模板管理
  templates: {
    cargoTemplates: CargoTemplate[];
    customContainers: CustomContainerSpec[];
  };
}
```

### 4.5 类型定义层

```typescript
// types/index.ts 核心类型
interface Cargo {
  id: string;
  name: string;
  dimensions: Dimensions;  // length, width, height (mm)
  weight: number;          // kg
  quantity: number;
  type: CargoType;         // box, pallet, bag, cylinder, etc.
  priority: Priority;      // 1-4
  stackable: boolean;
  maxStack: number;
  rotatable: boolean;
  fragile: boolean;
  thisSideUp: boolean;
  color?: string;
  description?: string;
}

interface ContainerSpec {
  id: ContainerType;
  name: string;
  description: string;
  innerDimensions: Dimensions;
  doorDimensions?: { width: number; height: number };
  maxPayload: number;      // 最大载重 (kg)
  tareWeight: number;      // 自重 (kg)
  maxWeight: number;       // 最大总重 (kg)
  volume: number;          // 立方米
  isStandard: boolean;
}

interface PlacedCargo extends Cargo {
  position: Position;      // 放置位置 (mm)
  rotation: RotationState; // 旋转状态
  placedQuantity: number;
  containerId: string;
}

interface PackingResult {
  id: string;
  createdAt: Date;
  containers: Container[];
  unplacedCargos: Cargo[];
  totalStats: {
    totalContainers: number;
    totalCargos: number;
    placedCargos: number;
    unplacedCargos: number;
    totalVolume: number;
    usedVolume: number;
    volumeUtilization: number;
    totalWeight: number;
  };
  algorithm: string;
  duration: number;        // 计算耗时 (ms)
}

interface CargoTemplate extends Omit<Cargo, 'id' | 'quantity'> {
  templateId: string;
  category: string;
  usageCount: number;
  createdAt: Date;
  updatedAt: Date;
}
```

---

## 五、关键流程图

### 5.1 装箱计算流程

```
用户选择货物 → 选择集装箱 → 配置算法 → 开始计算
                                    │
                                    ▼
                          算法调度器选择算法
                                    │
           ┌────────────────────────┼────────────────────────┐
           ▼                        ▼                        ▼
        FFD算法                  GA算法                   SA算法
           │                        │                        │
           ▼                        ▼                        ▼
      约束验证                  约束验证                  约束验证
           │                        │                        │
           └────────────────────────┼────────────────────────┘
                                    │
                                    ▼
                          3D可视化渲染 → 结果展示 → 导出报表
```

### 5.2 约束验证流程

```
输入: PlacedCargo[], ContainerSpec
           │
           ▼
    ┌───────────────┐
    │ 重量约束检查   │
    └───────┬───────┘
            │
            ▼
    ┌───────────────┐
    │ 边界约束检查   │
    └───────┬───────┘
            │
            ▼
    ┌───────────────┐
    │ 重心约束检查   │
    └───────┬───────┘
            │
            ▼
    ┌───────────────┐
    │ 堆码约束检查   │
    └───────┬───────┘
            │
            ▼
    ┌───────────────┐
    │ 方向约束检查   │
    └───────┬───────┘
            │
            ▼
    输出: { valid: boolean, violations: [], warnings: [] }
```

### 5.3 多柜优化流程

```
输入: Cargo[], AvailableContainerTypes[]
           │
           ▼
    ┌──────────────────┐
    │ 估算所需柜数     │ ← 按体积/重量估算
    └───────┬──────────┘
            │
            ▼
    ┌──────────────────┐
    │ 生成箱型组合     │ ← 尝试不同组合
    └───────┬──────────┘
            │
            ▼
    ┌──────────────────┐
    │ 评估组合优劣     │ ← 计算利用率、成本
    └───────┬──────────┘
            │
            ▼
    ┌──────────────────┐
    │ 选择最优组合     │ ← 返回推荐配置
    └───────┬──────────┘
            │
            ▼
    输出: { containers: [{type, count}], estimate }
```

---

## 六、API设计

### 6.1 算法API

```typescript
// 装箱算法接口
interface PackingAlgorithm {
  name: string;
  id: 'FFD' | 'BF' | 'GA' | 'SA' | 'MultiObjective';
  description: string;
  parameters: AlgorithmParameter[];
  
  pack(
    cargos: Cargo[],
    containerSpecs: ContainerSpec[],
    options?: PackingOptions
  ): PackingResult;
}

// 算法参数
interface AlgorithmParameter {
  name: string;
  key: string;
  type: 'number' | 'boolean' | 'select';
  default: unknown;
  min?: number;
  max?: number;
  options?: string[];
  description?: string;
}

// 装箱选项
interface PackingOptions {
  allowRotation?: boolean;
  maxContainers?: number;
  timeLimit?: number;        // 秒
  weights?: ObjectiveWeights;
}

// 多目标权重
interface ObjectiveWeights {
  utilization: number;       // 空间利用率权重
  balance: number;           // 重心平衡权重
  stacking: number;          // 堆叠稳定性权重
  loading: number;           // 装卸效率权重
}
```

### 6.2 约束API

```typescript
// 约束验证接口
interface ConstraintValidator {
  type: ConstraintType;
  validate(
    placedCargos: PlacedCargo[],
    containerSpec: ContainerSpec
  ): { valid: boolean; violations: ConstraintViolation[] };
}

// 约束类型
type ConstraintType = 
  | 'weight'           // 重量约束
  | 'boundary'         // 边界约束
  | 'centerOfGravity'  // 重心约束
  | 'stacking'         // 堆码约束
  | 'orientation';     // 方向约束

// 约束违反
interface ConstraintViolation {
  type: ConstraintType;
  message: string;
  severity: 'error' | 'warning';
  affectedCargoIds?: string[];
}
```

### 6.3 多柜优化API

```typescript
// 多柜优化配置
interface MultiContainerConfig {
  containerTypes?: ContainerType[];
  maxContainers?: number;
  optimizationGoal?: 'minContainers' | 'maxUtilization';
  groupingStrategy?: 'none' | 'weight' | 'destination' | 'priority';
  allowMixedTypes?: boolean;
}

// 推荐结果
interface ContainerRecommendation {
  containers: { type: ContainerType; count: number }[];
  estimate: number;
  utilization: number;
}
```

### 6.4 导出API

```typescript
// 导出格式
type ExportFormat = 'html' | 'csv' | 'json' | 'txt';

// 导出配置
interface ExportConfig {
  format: ExportFormat;
  includeImages?: boolean;
  include3DView?: boolean;
  includeStats?: boolean;
}

// 导出接口
interface ExportService {
  exportPackingList(result: PackingResult, config: ExportConfig): string;
  exportLoadingGuide(result: PackingResult): string;
  exportUtilizationReport(result: PackingResult): string;
}
```

---

## 七、性能优化策略

### 7.1 算法优化

| 优化项 | 策略 |
|--------|------|
| 空间分割 | 使用八叉树/四叉树加速空间查询 |
| 并行计算 | Web Worker 多线程计算 |
| 渐进式计算 | 分阶段返回结果 |
| 缓存机制 | 缓存相似问题的计算结果 |
| 参数调优 | GA/SA算法参数自适应调整 |

### 7.2 3D渲染优化

| 优化项 | 策略 |
|--------|------|
| 实例化渲染 | InstancedMesh 批量渲染 |
| LOD技术 | 多级细节层次 |
| 视锥剔除 | 只渲染可见物体 |
| 着色器优化 | GPU加速计算 |
| 响应式画布 | 根据设备性能调整渲染质量 |

### 7.3 内存管理

| 优化项 | 策略 |
|--------|------|
| 对象池 | 复用3D对象 |
| 垃圾回收 | 及时清理不再使用的资源 |
| 分批加载 | 大数据量分批处理 |
| 本地存储 | localStorage持久化模板数据 |

---

## 八、安全性考虑

### 8.1 输入验证

- 货物尺寸范围检查（0 < 尺寸 < 容器尺寸）
- 重量限制验证（0 < 重量 < 最大载重）
- 数量合理性校验（数量 >= 0）
- 算法参数边界检查

### 8.2 错误处理

- 算法异常捕获（超时、内存不足）
- 3D渲染错误恢复（WebGL上下文丢失）
- 用户友好的错误提示（本地化消息）
- 日志记录和监控

### 8.3 数据保护

- 本地存储加密（敏感配置）
- 敏感信息脱敏（日志中的用户数据）
- 操作日志记录（审计追踪）
- XSS防护（输入内容过滤）

---

## 九、部署与集成

### 9.1 部署架构

```
开发环境 → 测试环境 → 生产环境
    │           │           │
    ▼           ▼           ▼
  Vite Dev   Jest测试   Cloudflare Pages
```

### 9.2 CI/CD流程

```
代码提交 → GitHub Actions → 构建测试 → 自动部署
                                    │
                           ┌────────┴────────┐
                           ▼                 ▼
                      测试环境            生产环境
```

### 9.3 环境配置

| 环境 | URL | 说明 |
|------|-----|------|
| 开发 | http://localhost:3000 | 本地开发服务器 |
| 测试 | https://test.container-loading.example.com | 测试环境 |
| 生产 | https://container-loading.example.com | 生产环境 |

---

## 十、版本演进

| 版本 | 核心技术变更 | 关键文件 |
|------|------------|----------|
| V1.0 | 基础FFD算法、Three.js 3D渲染 | `ffd.ts`, `Container3D.tsx` |
| V1.1 | 货物选择功能、移动端适配 | `CargoManager.tsx`, `index.css` |
| V1.2 | 约束求解器、重心可视化 | `constraintSolver.ts`, `CenterOfGravity.tsx` |
| V1.3 | GA/SA算法、多目标优化 | `ga.ts`, `sa.ts`, `multiObjective.ts` |
| V1.4 | 多柜联合优化 | `multiContainer.ts` |
| V1.5 | 报表导出功能 | `exportUtils.ts`, `ExportPanel.tsx` |
| V1.6 | 货物模板库、自定义集装箱 | `templateManager.ts`, `containerSpecs.ts` |

---

## 十一、技术决策记录

| 决策 | 说明 | 原因 |
|------|------|------|
| **Zustand** | 使用Zustand作为状态管理 | 轻量级、无Boilerplate、React 18兼容 |
| **Three.js** | 使用Three.js进行3D渲染 | 成熟稳定、社区活跃、React封装完善 |
| **Vite** | 使用Vite作为构建工具 | 快速开发服务器、现代化构建 |
| **Ant Design** | 使用Ant Design作为UI组件库 | 组件丰富、设计规范一致 |
| **Web Worker** | 使用Web Worker进行算法计算 | 避免阻塞主线程、提升响应性 |

---

*文档更新时间：2026年5月24日*
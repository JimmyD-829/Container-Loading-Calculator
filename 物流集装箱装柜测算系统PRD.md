# 物流集装箱装柜测算系统 — 产品需求文档（PRD）

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档名称 | 物流集装箱装柜测算系统 — 产品需求文档 |
| 文档版本 | V1.1 |
| 编写日期 | 2026年5月20日 |
| 文档状态 | 迭代版本 |
| 编写人 | 产品团队 |
| 目标读者 | 产品经理、开发团队、测试团队、项目干系人 |
| 保密级别 | 内部公开 |

---

## 版本历史

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| V1.0 | 2026年5月16日 | 初始版本，包含货物管理、集装箱选择、装柜计算、3D可视化等核心功能 |
| V1.1 | 2026年5月20日 | 1. 修复标题遮挡问题；2. 增加货物选择功能，支持选择部分货物进行装载计算；3. 优化3D可视化界面尺寸 |
| V1.2 | 2026年5月24日 | 1. 算法增强（GA遗传算法、SA模拟退火）；2. 约束求解器（重心、堆码、方向约束）；3. 多目标优化框架 |
| V1.3 | 2026年5月24日 | 1. 多柜联合优化；2. 自动柜数估算；3. 混合箱型支持 |
| V1.4 | 2026年5月24日 | 1. 报表导出功能（Excel/CSV/JSON/TXT）；2. 分享功能；3. 装柜清单生成 |
| V1.5 | 2026年5月24日 | 1. 货物模板库；2. 自定义集装箱管理；3. 本地存储持久化 |
| V1.6 | 2026年5月24日 | 1. 修复顶部标题遮挡问题（使用原生header标签）；2. 完善导出报告功能（添加可行性分析）；3. 3D视图下方添加统计数据面板；4. 修复导出按钮点击无响应问题；5. 支持Excel/CSV/JSON/TXT四种导出格式 |
| V2.0 | 2026年5月25日 | 1. 3D可视化分层查看功能（逐层浏览、装柜动画）；2. 历史方案版本管理（自动保存、状态管理）；3. 历史方案对比分析功能；4. 历史数据查询功能（多条件筛选）；5. 方案复用功能（相似方案匹配、微调重算）；6. 数据权限控制；7. 操作审计日志 |

---

## 1. 项目概述

### 1.1 项目背景

随着全球化贸易的持续深入发展，集装箱运输已成为国际物流体系中最核心的运输方式。据国际海事组织（IMO）统计，全球每年超过80%的国际贸易货物通过集装箱进行运输。然而，在集装箱装柜环节，行业长期面临空间利用率偏低的困境。

根据行业调研数据，当前物流行业平均集装箱空间利用率仅为65%-70%，这意味着30%-35%的运费实际上浪费在了空柜运输上。以一条中国至欧洲的海运航线为例，一个40HQ集装箱的运费约为3000-8000美元，若空间利用率从65%提升至90%，单柜可节省运费约750-2100美元。对于一个年出口1000个集装箱的中型外贸企业而言，年节省运费可达75万-210万美元。

当前行业装柜作业主要依赖人工经验，存在以下突出问题：

1. **效率低下**：经验丰富的装柜工人需要花费30分钟至2小时来规划一个集装箱的装载方案，面对大批量多品类货物时，规划时间更长，严重影响发货时效。
2. **错误率高**：人工计算容易出现尺寸换算错误、重量估算偏差、重心失衡等问题，导致货物损坏、集装箱超重被拒载等事故。
3. **难以复用**：每次装柜都需要重新规划，历史经验和优秀方案无法有效沉淀和复用，造成人力成本的持续浪费。
4. **缺乏量化对比**：无法对不同装柜方案进行科学的量化对比，难以持续优化装柜策略。
5. **管理盲区**：管理层缺乏装柜作业的实时数据和历史统计，无法进行有效的成本控制和效率管理。

在数字化转型的大趋势下，企业对自动化、智能化装柜测算系统的需求日益迫切。通过引入智能算法和3D可视化技术，可以大幅提升集装箱空间利用率，降低物流运输成本，建立标准化的装柜作业流程，实现方案的可追溯、可对比、可复用。

### 1.2 项目目标

#### 1.2.1 核心目标

通过智能三维装箱算法，将集装箱空间利用率从行业平均的65%-70%提升至85%-95%，在保证货物安全和装卸效率的前提下，最大化利用每一个集装箱的装载空间。

#### 1.2.2 业务目标

| 目标维度 | 具体指标 | 衡量方式 |
|---------|---------|---------|
| 成本降低 | 物流运输成本降低15%以上 | 对比系统上线前后单柜平均运输成本 |
| 效率提升 | 装柜方案生成时间缩短80%以上 | 对比人工规划与系统计算耗时 |
| 柜数减少 | 同等货物量所需集装箱数量减少10%-20% | 统计系统优化前后的用柜数量 |
| 货损降低 | 因装柜不当导致的货损率降低50%以上 | 统计货损理赔数据 |

#### 1.2.3 管理目标

- 建立标准化的装柜作业流程，消除经验依赖
- 实现装柜方案的全生命周期管理：创建、计算、审核、执行、归档
- 建立装柜作业数据资产，支持历史方案查询、对比分析和趋势预测
- 提供多维度的运营报表，为管理层决策提供数据支撑

#### 1.2.4 技术目标

- 构建高可用、可扩展的装柜计算引擎，支持多种装箱算法的灵活切换
- 实现基于WebGL的高性能3D可视化渲染，支持大规模货物的实时展示
- 采用微服务架构，核心模块独立部署，支持水平扩展
- 提供标准RESTful API，支持与WMS、TMS、ERP等第三方系统的无缝集成

### 1.3 适用范围

#### 1.3.1 全品类货物支持

本系统支持以下全品类货物的装柜测算：

| 货物类别 | 典型物品 | 特殊约束 |
|---------|---------|---------|
| 标准箱型货物 | 纸箱、木箱、塑料箱 | 尺寸规则，支持6方向摆放 |
| 托盘货物 | 标准托盘（1200×1000mm）、美式托盘（1200×1200mm） | 整托不可拆分，底部不可悬空 |
| 不规则形状货物 | 圆桶、管道、钢材、线圈 | 等效包围盒计算，需额外间隙 |
| 易碎品 | 玻璃制品、陶瓷、精密仪器 | 不可堆码、需缓冲、顶部保护 |
| 冷链货物 | 冷冻食品、生鲜、药品 | 温控分区、通风间距要求 |
| 危险品 | 化学品、易燃易爆品 | 隔离要求、重量限制、合规检查 |
| 超大超重货物 | 机械设备、车辆部件 | 重心约束、绑扎点、承重分布 |

#### 1.3.2 全箱型支持

系统内置以下标准集装箱类型的参数，并支持自定义扩展：

- **干货箱**：20GP、40GP、40HQ（高柜）、45HQ
- **冷藏箱**：20RF、40RF、40RH（高柜冷藏）
- **开顶箱**：20OT、40OT
- **框架箱**：20FR、40FR
- **罐式箱**：特种液体运输
- **平板箱**：超高大件货物运输

#### 1.3.3 适用场景

| 场景 | 描述 | 特点 |
|------|------|------|
| 海运出口装柜 | 工厂或仓库装柜后运至港口 | 货物量大、品类多、需最大化利用率 |
| 铁路运输装柜 | 中欧班列等铁路集装箱运输 | 重量限制严格、震动约束 |
| 公路运输装柜 | 国内公路集装箱运输 | 多站配送、卸货顺序约束 |
| 仓储出库装柜 | 大批量订单出库装柜 | 与WMS系统集成、自动化程度高 |

### 1.4 术语定义

| 术语 | 英文全称 | 定义 |
|------|---------|------|
| 3D-BPP | 3D Bin Packing Problem | 三维装箱问题，指在给定的三维容器中装入尽可能多的三维物体，使空间利用率最大化 |
| CBM | Cubic Meter | 立方米，国际通用的体积计量单位 |
| TEU | Twenty-foot Equivalent Unit | 标准箱单位，以20英尺干货箱为基准的集装箱计量单位，1×40GP = 2 TEU |
| FEU | Forty-foot Equivalent Unit | 40英尺标准箱单位，1 FEU = 2 TEU |
| FFD | First Fit Decreasing | 首次适应递减算法，将货物按体积从大到小排序，依次放入第一个能容纳它的空间 |
| BFD | Best Fit Decreasing | 最佳适应递减算法，将货物按体积从大到小排序，放入剩余空间最小的能容纳它的空间 |
| GA | Genetic Algorithm | 遗传算法，模拟自然选择和遗传机制的启发式优化算法 |
| SA | Simulated Annealing | 模拟退火算法，模拟固体退火过程的全局优化算法 |
| SKU | Stock Keeping Unit | 库存量单位，用于唯一标识某一具体商品的最小单位 |
| WMS | Warehouse Management System | 仓储管理系统，负责仓库入库、出库、库存管理等 |
| TMS | Transportation Management System | 运输管理系统，负责运输计划、车辆调度、路线优化等 |
| ERP | Enterprise Resource Planning | 企业资源计划系统，集成企业各业务模块的综合管理系统 |
| LIFO | Last In First Out | 后进先出，多站配送场景下后卸的货物先装入 |
| COG | Center of Gravity | 重心，装载后货物与集装箱整体的重心位置 |
| WebGL | Web Graphics Library | 基于OpenGL的Web端3D图形渲染标准 |

---

## 2. 需求分析

### 2.1 用户角色分析

本系统面向物流供应链中的多个角色，各角色的职责、核心需求和使用频率如下：

| 角色 | 职责描述 | 核心需求 | 使用频率 | 优先级 |
|------|---------|---------|---------|--------|
| 装柜操作员 | 现场执行装柜作业，负责按方案将货物装入集装箱 | 获取装柜方案、查看3D装箱示意图、按装卸指导书操作、反馈实际装柜情况 | 每日高频（5-20次/天） | P0 |
| 物流调度员 | 安排运输计划、分配集装箱资源、协调发货时间 | 快速测算所需柜数、优化柜型选择、多方案对比、批量任务处理 | 每日中频（3-10次/天） | P0 |
| 仓库管理员 | 管理仓库库存、协调出库作业、维护货物信息 | 货物信息管理、批量导入货物数据、装柜清单导出、库存联动 | 每日中频（2-5次/天） | P1 |
| 物流主管 | 监控运营效率、审批装柜方案、分析成本数据 | 利用率报表、历史数据分析、成本核算、团队绩效管理 | 每周低频（2-5次/周） | P1 |
| 系统管理员 | 维护系统配置、管理用户账号、监控系统运行 | 用户权限管理、数据字典维护、系统参数配置、日志审计 | 每月低频（1-5次/月） | P2 |

#### 2.1.1 用户画像

**装柜操作员 — 张师傅**
- 年龄：35-50岁
- 工作经验：10年以上装柜经验
- 技术水平：会使用智能手机，电脑操作基础一般
- 核心诉求：方案要简单易懂，3D图要直观，能打印出来带到现场对照操作
- 痛点：之前全凭经验装柜，经常装到一半发现空间不够要重新调整

**物流调度员 — 李经理**
- 年龄：28-40岁
- 工作经验：5年以上物流调度经验
- 技术水平：熟练使用办公软件和物流系统
- 核心诉求：能快速给出装柜方案和所需柜数，方便与客户报价和安排运输
- 痛点：客户临时加货或改单时，需要快速重新计算，人工计算来不及

**物流主管 — 王总**
- 年龄：35-50岁
- 工作经验：15年以上物流管理经验
- 核心诉求：能看到整体运营数据，知道哪里有浪费，能做成本分析
- 痛点：缺乏数据支撑，无法量化装柜效率，难以向管理层汇报改进成果

### 2.2 业务流程分析

#### 2.2.1 核心业务流程（装柜作业全流程）

以下是从订单接收到装柜完成的完整业务流程，共包含11个关键步骤：

```
步骤1：订单接收
    ↓
步骤2：货物信息录入/同步
    ↓
步骤3：货物属性配置
    ↓
步骤4：创建装柜任务
    ↓
步骤5：选择集装箱类型
    ↓
步骤6：设置约束条件
    ↓
步骤7：执行装柜计算
    ↓
步骤8：方案评审与选择
    ↓
步骤9：生成装柜指导文件
    ↓
步骤10：现场装柜执行
    ↓
步骤11：装柜确认与归档
```

**步骤1：订单接收**
- 来源：ERP销售订单、WMS出库单、客户手工下单
- 内容：订单号、客户信息、货物清单、目的地、要求发货日期
- 处理：系统自动接收或人工录入订单信息

**步骤2：货物信息录入/同步**
- 方式一：从WMS/ERP自动同步SKU属性（尺寸、重量、包装类型）
- 方式二：通过Excel模板批量导入货物信息
- 方式三：在系统中手工录入单件货物信息
- 校验：必填字段检查、数据格式验证、重复数据检测

**步骤3：货物属性配置**
- 设置每种货物的摆放方向限制（6种方向独立开关）
- 配置最大堆码层数和底部承托面积要求
- 标记易碎品、怕压货物、重货等特殊属性
- 设置货物分组（按订单号、目的地、客户、优先级）

**步骤4：创建装柜任务**
- 选择本次装柜涉及的货物（从货物清单中勾选）
- 设置任务名称、关联订单号、计划装柜日期
- 指定装柜场景（海运/铁路/公路/仓储出库）

**步骤5：选择集装箱类型**
- 从标准柜型库选择（20GP、40GP、40HQ等）
- 或使用自定义集装箱参数
- 支持选择多种柜型进行联合优化
- 系统根据货物总体积和总重量推荐合适柜型

**步骤6：设置约束条件**
- 重量约束：确认最大载重限制
- 重心约束：设置重心偏移允许范围
- 堆码约束：确认堆码层数和承重限制
- 互斥约束：设置不可相邻放置的货物
- 卸货顺序约束：多站配送场景设置各站卸货清单
- 特殊约束：冷链通风间距、危险品隔离等

**步骤7：执行装柜计算**
- 选择计算算法（自动推荐或手动选择）
- 设置计算时间限制
- 系统执行装箱算法计算
- 返回计算结果：空间利用率、重量利用率、各货物摆放位置

**步骤8：方案评审与选择**
- 查看3D装箱可视化效果
- 对比多个方案的利用率、成本、约束满足度
- 检查重心位置是否在安全范围内
- 必要时调整约束条件重新计算
- 选择最终方案或锁定方案

**步骤9：生成装柜指导文件**
- 生成装柜清单（Excel/PDF）
- 生成装柜示意图（2D/3D）
- 生成装卸指导书（PDF，适合现场打印）
- 分发给装柜操作员

**步骤10：现场装柜执行**
- 操作员按装卸指导书逐层装柜
- 实际装柜过程中如遇问题可反馈调整
- 记录实际装柜情况（拍照、备注）

**步骤11：装柜确认与归档**
- 确认装柜完成，记录实际装载数据
- 计算实际利用率与计划利用率的偏差
- 方案归档，进入历史方案库
- 数据回传WMS/TMS/ERP系统

### 2.3 痛点分析

| 序号 | 痛点 | 详细描述 | 影响范围 | 影响程度 | 优先级 |
|------|------|---------|---------|---------|--------|
| 1 | 空间利用率低 | 人工经验装柜空间利用率仅60%-70%，大量空间被浪费 | 所有使用集装箱运输的企业 | 运费浪费严重，年损失可达数十万至数百万 | P0 |
| 2 | 方案生成效率低 | 手工计算一个集装箱的装载方案需30分钟-2小时，无法快速响应客户询价和临时变更 | 物流调度员、装柜操作员 | 错过发货时效，客户满意度下降 | P0 |
| 3 | 货物安全隐患 | 人工规划难以精确计算重心位置，可能导致运输途中货物倾倒、集装箱超重被拒载 | 装柜操作员、物流企业 | 货物损坏赔偿、运输安全事故 | P0 |
| 4 | 方案不可复用 | 每次装柜都需重新规划，历史优秀方案无法有效复用 | 物流调度员、装柜操作员 | 人力成本高，效率低下 | P1 |
| 5 | 缺乏方案对比 | 无法科学对比不同装柜方案的优劣，难以持续优化 | 物流主管、物流调度员 | 无法量化改进效果，优化方向不明确 | P1 |
| 6 | 数据不透明 | 缺乏历史装柜数据和统计分析，管理层无法掌握运营全貌 | 物流主管、企业管理层 | 管理决策缺乏数据支撑 | P2 |
| 7 | 多柜规划困难 | 大批量货物需要多个集装箱时，人工难以全局优化分柜策略 | 物流调度员 | 集装箱数量浪费，总成本增加 | P1 |
| 8 | 多站配送复杂 | 多站配送场景需考虑卸货顺序（后卸先装），人工规划极易出错 | 物流调度员、装柜操作员 | 到站卸货困难，作业效率低 | P1 |
| 9 | 系统集成缺失 | 装柜系统与WMS/TMS/ERP系统割裂，数据需手工重复录入 | 仓库管理员、物流调度员 | 数据一致性差，工作效率低 | P2 |
| 10 | 缺乏操作标准 | 不同操作员装柜标准不一，质量参差不齐 | 物流主管、装柜操作员 | 装柜质量不稳定，难以管控 | P2 |

---

## 3. 功能需求

### 3.1 货物管理模块

#### 3.1.1 货物信息录入

系统提供完整的货物信息录入功能，支持录入以下信息字段：

**基本信息**
- 货物名称：必填，最长100个字符，支持中英文
- 货物描述：选填，最长500个字符
- SKU编码：必填，唯一标识，最长50个字符
- 条形码：选填，支持EAN-13、Code-128等格式
- 货物分类：选填，支持多级分类（如：电子产品 > 手机 > 智能手机）

**尺寸信息**
- 长度（mm）：必填，正数，精度1mm，范围1-20000mm
- 宽度（mm）：必填，正数，精度1mm，范围1-20000mm
- 高度（mm）：必填，正数，精度1mm，范围1-20000mm
- 体积（CBM）：系统自动计算，公式：长 × 宽 × 高 / 1,000,000,000，保留4位小数
- 尺寸备注：选填，记录特殊尺寸说明（如含托盘总高度）

**重量信息**
- 毛重（kg）：必填，正数，精度0.1kg，范围0.1-50000kg
- 净重（kg）：选填，正数，精度0.1kg
- 单位重量（kg/CBM）：系统自动计算，用于密度排序

**数量信息**
- 件数：必填，正整数，范围1-99999
- 总重量（kg）：系统自动计算，公式：毛重 × 件数
- 总体积（CBM）：系统自动计算，公式：单件体积 × 件数

**包装类型**
- 纸箱（Carton）：最常见的包装类型，支持6方向摆放
- 木箱（Wooden Case）：承重能力强，通常不可倒置
- 托盘（Pallet）：整托不可拆分，底部不可悬空
- 散装（Bulk）：无固定包装，需特殊处理
- 裸装（Unpacked）：无包装的大件货物
- 桶装（Drum）：圆柱形，需等效包围盒计算
- 管材（Pipe/Tube）：长条形，需特殊摆放约束

**货物分组**
- 按订单号分组：同一订单的货物归为一组
- 按目的地分组：运往同一目的地的货物归为一组
- 按客户分组：同一客户的货物归为一组
- 按优先级分组：紧急/普通/低优先级
- 自定义分组：用户可创建自定义分组规则

#### 3.1.2 货物属性配置

每种货物除了基本信息外，还需配置以下装柜约束属性：

**摆放方向控制**
- 系统提供6种摆放方向的独立开关控制：
  - 方向1：原始方向（长×宽×高）
  - 方向2：长×高×宽（宽高互换）
  - 方向3：宽×长×高（长宽互换）
  - 方向4：宽×高×长（长高互换）
  - 方向5：高×长×宽（宽高互换）
  - 方向6：高×宽×长（长宽互换）
- 默认：纸箱类货物6方向全部开启，木箱/托盘类仅开启原始方向
- 快捷设置：提供"仅原始方向""允许翻转""全部方向"三个快捷选项

**堆码约束**
- 最大堆码层数：1-20层可配置，默认值为根据货物类型自动设置（纸箱默认8层，木箱默认5层，托盘默认3层）
- 底部最小承托面积比例：0%-100%可配置，默认75%。例如设置为80%，则下方货物的上表面积至少需覆盖当前货物底面积的80%
- 是否可承重：布尔值，标记该货物上方是否可放置其他货物。默认为是，易碎品和精密仪器默认为否
- 最大承重：选填，该货物最多能承受的上方货物总重量（kg）

**特殊属性标记**
- 是否易碎：标记易碎品，触发以下特殊约束——不可堆码、优先放置在底层、周围需留缓冲空间
- 是否怕压：标记怕压货物，触发约束——上方不可放置重货、堆码层数限制为1-2层
- 是否重货：标记单件重量超过500kg的重货，触发约束——必须放置在底层、重心校验加强
- 是否液体：标记桶装液体，触发约束——必须直立放置、不可倒置
- 是否危险品：标记危险品，触发约束——需隔离区域、与不相容货物互斥

**其他属性**
- 变形公差：允许的尺寸误差范围（mm），默认0mm。设置为5mm表示货物尺寸可以有±5mm的误差
- 货物价值：选填，用于优先级排序和保险计算，单位为人民币元
- 装卸优先级：1-10级，1为最高优先级，数值越大优先级越低。高优先级货物优先安排装柜
- 温度要求：选填，冷链货物需指定存储温度范围（如-18°C ~ -15°C）
- 通风要求：选填，冷链货物需指定通风间距（如四周各留50mm）

#### 3.1.3 批量导入

为提高大批量货物信息的录入效率，系统提供Excel和CSV格式的批量导入功能：

**支持格式**
- Excel文件：.xlsx（推荐）、.xls
- CSV文件：.csv（UTF-8编码，逗号分隔）

**标准导入模板**
- 系统提供标准导入模板下载（Excel格式）
- 模板包含以下字段列：SKU编码、货物名称、长(mm)、宽(mm)、高(mm)、毛重(kg)、件数、包装类型、最大堆码层数、是否易碎、是否怕压、备注
- 模板中包含字段说明sheet和数据填写示例sheet
- 必填字段以红色表头标注

**导入数据校验**
- 必填字段检查：SKU编码、货物名称、长、宽、高、毛重、件数不可为空
- 数据格式验证：尺寸和重量必须为正数，件数必须为正整数
- 逻辑校验：体积不能超过最大集装箱内部容积，单件重量不能超过最大载重
- 重复数据检测：同一批次中SKU编码不可重复，与已有数据的SKU编码冲突时给出提示
- 尺寸合理性检查：长宽高比例异常（如某维度超过其他维度20倍以上）时给出警告

**导入结果反馈**
- 导入完成后显示导入结果摘要：总条数、成功条数、失败条数、警告条数
- 失败数据明细：逐条显示失败原因（如"第5行：毛重不能为空"）
- 警告数据明细：显示警告信息（如"第12行：长宽比超过20:1，请确认尺寸是否正确"）
- 支持下载失败/警告数据明细（Excel格式），方便用户修正后重新导入

**导入模式**
- 增量导入（默认）：新增货物数据，已存在的SKU编码跳过或更新（用户选择）
- 覆盖导入：清空当前任务的所有货物数据后重新导入（需二次确认）

#### 3.1.4 货物模板库

为减少重复录入工作，系统提供货物模板库功能：

**模板保存**
- 将当前编辑的货物信息一键保存为模板
- 模板保存时需填写：模板名称（必填）、模板分类（选填）、适用场景说明（选填）
- 模板包含货物的所有信息（基本信息、尺寸、重量、属性配置）

**模板分类管理**
- 按品类分类：电子产品、日用百货、食品饮料、机械设备、化工品、服装纺织等
- 按客户分类：按客户名称分组管理
- 按包装类型分类：纸箱类、木箱类、托盘类、桶装类等
- 支持自定义分类标签

**模板快速引用**
- 新建装柜任务时，可从模板库中快速选择货物模板
- 支持搜索模板：按名称、SKU编码、分类关键词搜索
- 选择模板后自动填充货物信息，用户可在此基础上修改
- 支持批量选择多个模板一次性添加

**模板版本管理**
- 每次修改模板自动生成新版本
- 版本号格式：V1.0、V1.1、V2.0
- 可查看历史版本的模板内容
- 支持回退到指定历史版本
- 模板变更记录：记录修改人、修改时间、修改内容

### 3.2 集装箱管理模块

#### 3.2.1 标准柜型库

系统内置国际标准集装箱参数，用户可直接选择使用。以下为完整的标准集装箱规格参数：

**干货箱（Dry Container）**

| 柜型 | 外部尺寸 L×W×H (mm) | 内部尺寸 L×W×H (mm) | 门框尺寸 W×H (mm) | 内部容积 (CBM) | 皮重 (kg) | 最大载重 (kg) | 最大总重 (kg) |
|------|---------------------|---------------------|-------------------|---------------|----------|-------------|-------------|
| 20GP | 6058×2438×2591 | 5898×2352×2393 | 2340×2280 | 33.2 | 2230 | 21770 | 24000 |
| 40GP | 12192×2438×2591 | 12032×2352×2393 | 2340×2280 | 67.7 | 3740 | 26680 | 30420 |
| 40HQ | 12192×2438×2896 | 12032×2352×2698 | 2340×2585 | 76.3 | 3940 | 26460 | 30400 |
| 45HQ | 13716×2438×2896 | 13556×2352×2698 | 2340×2585 | 86.1 | 4820 | 25580 | 30400 |

**冷藏箱（Reefer Container）**

| 柜型 | 外部尺寸 L×W×H (mm) | 内部尺寸 L×W×H (mm) | 门框尺寸 W×H (mm) | 内部容积 (CBM) | 皮重 (kg) | 最大载重 (kg) | 最大总重 (kg) |
|------|---------------------|---------------------|-------------------|---------------|----------|-------------|-------------|
| 20RF | 6058×2438×2591 | 5443×2289×2245 | 2276×2134 | 27.9 | 3060 | 20940 | 24000 |
| 40RF | 12192×2438×2591 | 11577×2289×2245 | 2276×2134 | 58.9 | 4760 | 25660 | 30420 |
| 40RH | 12192×2438×2896 | 11577×2289×2548 | 2276×2438 | 67.2 | 4960 | 25460 | 30420 |

**开顶箱（Open Top Container）**

| 柜型 | 外部尺寸 L×W×H (mm) | 内部尺寸 L×W×H (mm) | 门框尺寸 W×H (mm) | 内部容积 (CBM) | 皮重 (kg) | 最大载重 (kg) | 最大总重 (kg) |
|------|---------------------|---------------------|-------------------|---------------|----------|-------------|-------------|
| 20OT | 6058×2438×2591 | 5898×2352×2352 | 2340×2280 | 32.5 | 2360 | 21640 | 24000 |
| 40OT | 12192×2438×2591 | 12032×2352×2352 | 2340×2280 | 66.4 | 3820 | 26600 | 30420 |

**框架箱（Flat Rack Container）**

| 柜型 | 外部尺寸 L×W×H (mm) | 内部尺寸 L×W×H (mm) | 内部容积 (CBM) | 皮重 (kg) | 最大载重 (kg) | 最大总重 (kg) |
|------|---------------------|---------------------|---------------|----------|-------------|-------------|
| 20FR | 6058×2438×2591 | 5620×2178×2070 | 25.3 | 2870 | 21130 | 24000 |
| 40FR | 12192×2438×2591 | 11760×2178×2070 | 53.1 | 5300 | 25120 | 30420 |

> **注**：以上参数为国际标准参考值，实际集装箱参数可能因制造商和批次不同略有差异。系统允许用户在标准参数基础上进行微调。

#### 3.2.2 自定义集装箱

除标准柜型外，系统支持用户录入非标集装箱参数：

**自定义字段**
- 箱型名称：必填，如"散货船舱"、"厢式货车"
- 内部尺寸：长×宽×高（mm），必填
- 门框尺寸：宽×高（mm），选填（无门框的敞口容器可不填）
- 最大载重（kg）：必填
- 皮重（kg）：选填
- 形状描述：选填，如"矩形"、"U形"、"圆柱形"
- 备注：选填，记录特殊说明

**自定义箱型管理**
- 保存至企业柜型库，仅本企业用户可见
- 支持编辑和删除自定义箱型
- 删除前检查是否有关联的装柜任务，有关联时禁止删除并提示
- 支持将自定义箱型导出为参数文件，方便在其他环境导入

#### 3.2.3 可用容量计算

系统根据集装箱参数和装柜场景，自动计算实际可用容量：

**可用高度计算**
- 扣除托盘高度：如使用标准托盘（高度150mm），可用高度 = 内部高度 - 150mm
- 扣除绑扎/固定材料占用空间：顶部预留50-100mm用于绑扎带和固定材料
- 扣除通风间距（冷链场景）：顶部预留100-200mm用于冷风循环

**可用容积计算**
- 可用容积 = 可用长度 × 可用宽度 × 可用高度
- 扣除门框入口限制：如果门框宽度或高度小于内部尺寸，需考虑货物能否通过门框放入
- 扣除内壁凸起/凹槽：考虑集装箱内壁的加强筋、通风口等凸起物占用空间（通常预留10-20mm）

**可用载重计算**
- 可用载重 = 最大载重 - 固定材料重量 - 托盘重量
- 多站配送场景：需确保每个卸货站的累计重量不超过道路限重

**容量信息展示**
- 在集装箱选择界面清晰展示：内部容积、可用容积、最大载重、可用载重
- 货物总体积/总重量与可用容量的对比进度条
- 容量预警：当货物总体积超过可用容积90%或总重量超过可用载重90%时，以黄色预警提示

### 3.3 装柜计算引擎（核心模块）

装柜计算引擎是本系统的核心模块，负责根据货物信息、集装箱参数和约束条件，计算出最优的装箱方案。

#### 3.3.1 算法引擎

系统支持多种装箱算法，用户可根据场景需求选择合适的算法：

**FFD（首次适应递减算法）**

- **算法原理**：将所有货物按体积从大到小排序，依次将每个货物放入第一个能容纳它的空间位置。空间采用三维空间分割策略管理，每次放置货物后将剩余空间分割为最多三个子空间。
- **时间复杂度**：O(n log n)（排序阶段）+ O(n × m)（放置阶段，m为当前空间数量）
- **典型利用率**：70%-82%
- **适用场景**：实时快速估算、货物种类较少（<50种）、对计算速度要求高的场景
- **优势**：计算速度快，结果稳定可复现
- **劣势**：容易产生碎片空间，利用率不如元启发式算法

**BFD（最佳适应递减算法）**

- **算法原理**：将所有货物按体积从大到小排序，依次将每个货物放入"剩余空间最小"的能容纳它的空间位置。即选择放置后剩余空间最小的空间，减少碎片浪费。
- **时间复杂度**：O(n log n) + O(n × m log m)
- **典型利用率**：72%-85%
- **适用场景**：实时快速估算、对利用率有一定要求的场景
- **优势**：相比FFD减少了碎片空间，利用率更高
- **劣势**：计算时间略长于FFD，空间搜索开销更大

**GA（遗传算法）**

- **算法原理**：模拟自然选择和遗传机制的元启发式优化算法。将装箱方案编码为染色体（基因序列表示货物的放置顺序和方向），通过选择、交叉、变异等遗传操作在解空间中搜索最优解。
  - **编码方式**：排列编码，每个基因表示一个货物的放置顺序和方向
  - **适应度函数**：综合考虑空间利用率、重心偏移、约束违反惩罚
  - **选择算子**：锦标赛选择（Tournament Selection），锦标赛大小为3-5
  - **交叉算子**：顺序交叉（OX）或部分映射交叉（PMX）
  - **变异算子**：交换变异（随机交换两个货物的位置）、方向变异（随机改变一个货物的摆放方向）
  - **精英保留**：每代保留最优的5%个体直接进入下一代
- **时间复杂度**：O(n²) ~ O(n³)（取决于种群大小和迭代次数）
- **典型利用率**：85%-95%
- **适用场景**：离线精确优化、货物种类多（50-500种）、对利用率要求高的场景
- **优势**：全局搜索能力强，能找到接近最优的解
- **劣势**：计算时间较长，结果有一定随机性

**SA（模拟退火算法）**

- **算法原理**：模拟固体退火过程的全局优化算法。从一个初始解出发，在邻域中随机产生新解，以一定的概率接受劣解以跳出局部最优。随着"温度"逐渐降低，接受劣解的概率逐渐减小，最终收敛到近似最优解。
  - **初始温度**：根据初始解的适应度自动设定
  - **降温策略**：指数降温 T(k+1) = α × T(k)，α = 0.95-0.99
  - **邻域操作**：随机交换两个货物的位置、随机改变一个货物的摆放方向、随机移动一个货物到新位置
  - **终止条件**：温度降至终止温度或达到最大迭代次数
- **时间复杂度**：O(n²) ~ O(n³)
- **典型利用率**：83%-93%
- **适用场景**：约束复杂的单柜优化、需要在有限时间内获得较好解的场景
- **优势**：不易陷入局部最优，对初始解依赖性低
- **劣势**：参数调优较敏感，不同参数组合对结果影响较大

**算法自动推荐**

系统根据以下因素自动推荐最优算法：

| 货物种类数 | 货物总件数 | 约束复杂度 | 推荐算法 | 预计计算时间 |
|-----------|-----------|-----------|---------|------------|
| ≤ 10 | ≤ 50 | 低 | FFD | < 1秒 |
| ≤ 30 | ≤ 200 | 中 | BFD | < 2秒 |
| ≤ 100 | ≤ 1000 | 中 | GA（小种群） | < 30秒 |
| > 100 | > 1000 | 高 | 混合策略 | < 120秒 |
| 任意 | 任意 | 高（多约束） | SA | < 60秒 |

**多算法并行计算**

- 用户可选择"多算法并行"模式
- 系统同时启动FFD、BFD、GA、SA四种算法进行计算
- 各算法独立运行，互不干扰
- 所有算法完成后，自动对比各算法结果，返回综合最优解
- 并行计算充分利用多核CPU资源，总耗时约等于最慢算法的耗时

**计算时间控制**

用户可设置最大计算时间，系统在超时后返回当前已找到的最优解：

| 预设选项 | 最大计算时间 | 适用场景 |
|---------|------------|---------|
| 快速估算 | 10秒 | 客户询价、快速预览 |
| 标准计算 | 30秒 | 日常装柜方案生成 |
| 精确优化 | 60秒 | 重要货物、高价值运输 |
| 深度优化 | 120秒 | 大批量多柜联合优化 |
| 极限优化 | 300秒 | 离线规划、极致利用率追求 |

#### 3.3.2 约束求解器

约束求解器负责确保生成的装箱方案满足所有物理和业务约束：

**重量约束（硬约束）**
- 集装箱内所有货物的总重量不得超过集装箱的最大载重
- 公式：Σ(cargo_weight × cargo_quantity) ≤ container_max_payload
- 违反处理：方案直接判定为不可行

**重心约束（软约束，可配置为硬约束）**

装载完成后，需计算整体重心位置并验证是否在安全范围内：

- 纵向重心偏移（X轴）：|X_cog - L/2| ≤ L × 5%
  - 其中X_cog为重心X坐标，L为集装箱内部长度
  - 即重心偏离集装箱中心的距离不超过长度的5%
- 横向重心偏移（Y轴）：|Y_cog - W/2| ≤ W × 10%
  - 其中Y_cog为重心Y坐标，W为集装箱内部宽度
- 垂直重心高度（Z轴）：Z_cog ≤ H × 60%
  - 即重心高度不超过集装箱内部高度的60%
  - 此约束对公路运输尤为重要，防止转弯时侧翻
- 重心计算公式：
  ```
  X_cog = Σ(m_i × x_i) / Σ(m_i)
  Y_cog = Σ(m_i × y_i) / Σ(m_i)
  Z_cog = Σ(m_i × z_i) / Σ(m_i)
  ```
  其中 m_i 为第i个货物的重量，x_i/y_i/z_i 为第i个货物的重心坐标

**堆码约束（硬约束）**
- 货物堆码层数不超过其配置的最大堆码层数
- 上方货物的重量不超过当前货物的最大承重限制
- 标记为"不可承重"的货物上方不得放置任何货物
- 底部承托面积不低于设定阈值（默认75%）

**方向约束（硬约束）**
- 货物的摆放方向必须在允许的方向列表中
- 例如标记为"不可倒置"的货物，只能使用方向1-3（高度方向不变）

**悬空约束（硬约束）**
- 货物底部必须有足够的支撑，不允许大面积悬空
- 支撑面积比例 = 货物底面与下方货物/集装箱底面的接触面积 / 货物底面积
- 支撑面积比例必须 ≥ 配置的最小承托面积比例（默认75%）
- 特例：放置在集装箱底板的货物无需检查悬空约束

**互斥约束（硬约束）**
- 特定货物不可相邻放置（共享一个接触面）
- 典型场景：食品与化工品不可相邻、不同温度要求的冷链货物不可混装
- 互斥规则可配置：用户可定义货物分组之间的互斥关系

**卸货顺序约束（软约束）**
- 多站配送场景下，需遵循"后卸先装"（LIFO）原则
- 后卸货站的货物应放置在先卸货站货物的上方或靠近柜门的位置
- 系统根据配送站点的顺序自动生成卸货优先级
- 完全满足LIFO可能降低空间利用率，用户可权衡调整

#### 3.3.3 多目标优化

装柜问题本质上是一个多目标优化问题，系统提供灵活的多目标优化配置：

**优化目标**
- 主目标：空间利用率最大化
  - 空间利用率 = 已使用体积 / 集装箱可用容积 × 100%
- 次目标1：重心平衡
  - 重心偏移量最小化
- 次目标2：装卸效率
  - 大件/重货优先放置在靠近柜门的位置，减少搬运距离
- 次目标3：货物完整性
  - 易碎品放置在底层、减少堆码、远离柜门防撞击

**权重配置**

用户可通过滑块调整各优化目标的权重比例：

| 目标 | 默认权重 | 权重范围 | 说明 |
|------|---------|---------|------|
| 空间利用率 | 60% | 20%-100% | 权重越高，算法越倾向于最大化空间利用 |
| 重心平衡 | 20% | 0%-50% | 权重越高，重心越接近集装箱中心 |
| 装卸效率 | 10% | 0%-30% | 权重越高，重货越靠近柜门 |
| 货物完整性 | 10% | 0%-30% | 权重越高，易碎品保护越好 |

**Pareto最优解集**

- 当使用GA或SA算法时，系统可返回多个Pareto最优解
- Pareto最优解的定义：不存在另一个解在所有目标上都不劣于它
- 用户可从Pareto解集中选择最符合实际需求的方案
- 系统以散点图展示Pareto前沿，横轴为空间利用率，纵轴为重心偏移量

#### 3.3.4 多柜联合优化

当一批货物无法装入单个集装箱时，系统提供多柜联合优化功能：

**输入**
- 一批货物的完整信息（种类、尺寸、重量、数量、属性）
- 可用的集装箱类型（可多选）
- 约束条件（同单柜计算）
- 优化目标（最少柜数 / 最低总成本 / 最高总利用率）

**输出**
- 最优的集装箱数量和箱型组合
- 每个集装箱的详细装箱方案
- 总体利用率统计

**混合箱型优化**
- 同一批货物可使用不同箱型的集装箱
- 例如：大件使用40HQ，小件使用20GP
- 系统自动计算不同箱型组合的总成本和总利用率
- 推荐成本最低或利用率最高的箱型组合

**柜型推荐逻辑**
```
1. 计算货物总体积V_total和总重量W_total
2. 筛选满足载重要求的候选柜型（max_payload ≥ W_total）
3. 计算每种候选柜型的最少柜数：N = ceil(V_total / 可用容积)
4. 评估每种柜型组合的总成本 = N × 单柜运费
5. 考虑混合箱型：尝试大柜+小柜的组合
6. 返回成本最低的前3种柜型组合供用户选择
```

**分柜策略**
- 按重量分柜：确保每个集装箱不超重
- 按体积分柜：确保每个集装箱空间充分利用
- 按目的地分柜：运往同一目的地的货物尽量装入同一集装箱
- 按客户分柜：同一客户的货物尽量装入同一集装箱
- 按优先级分柜：紧急货物优先安排装柜

#### 3.3.5 3D空间分割算法

装柜计算引擎的核心是3D空间的高效管理，系统采用以下空间分割策略：

**Extreme Point方法**
- 每次放置一个货物后，在其六个面（上、前、左、右、后、下）的极端点生成新的候选放置位置
- 极端点定义：沿某个坐标轴方向延伸所能到达的最远可放置点
- 每个候选位置需验证：不与已放置货物重叠、不超出集装箱边界、满足悬空约束

**Maximal Space方法**
- 维护一个"最大剩余空间"列表
- 每个最大剩余空间是一个不被任何已放置货物占据的最大矩形区域
- 放置货物后，更新受影响的最大剩余空间
- 合并相邻且可连通的最大剩余空间，减少空间碎片

**碎片空间利用策略**
- 在主要货物放置完成后，系统自动扫描剩余的碎片空间
- 将小件货物（体积 < 集装箱容积的1%）尝试填入碎片空间
- 碎片填充采用贪心策略：优先填充最小的碎片空间

**空间合并算法**
- 定期检测相邻的空余空间是否可以合并为一个更大的空间
- 合并条件：两个空间共享一个完整的面，且合并后的空间不被任何已放置货物占据
- 空间合并可以有效减少碎片空间数量，提高后续货物的放置成功率

### 3.4 3D可视化模块

#### 3.4.1 三维装箱预览

系统提供基于WebGL的高性能3D装箱可视化功能：

**渲染引擎**
- 使用WebGL 2.0进行硬件加速渲染
- 支持数千个货物的高效渲染（基于实例化渲染技术 Instanced Rendering）
- 集装箱外壳采用半透明材质渲染，可透视查看内部货物

**颜色编码方案**
- 不同SKU的货物使用不同颜色区分
- 系统内置24种高区分度的颜色方案
- 用户可自定义货物颜色
- 颜色图例：在3D视图旁显示颜色与货物的对应关系

**透明度调节**
- 集装箱外壳透明度可调节：0%（完全透明）到 100%（完全不透明）
- 默认透明度：30%，既能看到外壳轮廓又能看到内部货物
- 支持一键切换：显示外壳/隐藏外壳/仅显示外壳

**货物标注**
- 在3D视图中显示货物的关键信息标注
- 标注内容可配置：货物名称、SKU编码、尺寸、重量、摆放坐标
- 标注采用引线方式，避免遮挡货物
- 支持按需显示/隐藏标注

#### 3.4.2 交互操作

**视角控制**
- 360度自由旋转：鼠标左键拖拽旋转视角
- 缩放查看：鼠标滚轮缩放，支持触摸屏双指缩放
- 平移查看：鼠标右键拖拽平移视角
- 预设视角：正面、背面、左侧、右侧、顶部、底部、等轴测视图，一键切换

**货物交互**
- 点击选中：鼠标左键点击货物，高亮选中并显示详细信息面板
- 悬停高亮：鼠标悬停在货物上时，货物边缘高亮显示
- 双击聚焦：双击货物，视角自动聚焦到该货物并放大
- 右键菜单：右键点击货物弹出操作菜单（查看详情、隐藏、隔离显示）

**分层查看**
- 按层展示货物摆放情况
- 层高可自定义（默认每500mm为一层）
- 支持逐层浏览：上一层/下一层按钮，或拖动滑块快速切换
- 每层显示该层货物的俯视图和货物清单
- 分层视图支持动画播放：自动逐层展示，模拟装柜过程

#### 3.4.3 重心可视化

**重心位置显示**
- 在3D视图中以十字标记（+）显示装载后的重心位置
- 重心标记使用醒目的红色，大小随缩放自适应
- 显示重心三维坐标数值（精确到mm）

**重心偏移量显示**
- 显示重心在X、Y、Z三个方向上的偏移量
- 偏移量以数值和百分比两种方式展示
- 偏移量在安全范围内显示为绿色，接近临界值显示为黄色，超出范围显示为红色

**重心安全区域标识**
- 在3D视图中用半透明区域标识重心的安全范围
- 安全区域为一个以集装箱中心为基准的长方体区域
- 重心标记在安全区域内为绿色，在安全区域外为红色并闪烁告警

#### 3.4.4 碰撞检测

系统在3D可视化中内置碰撞检测功能，帮助用户发现方案中的潜在问题：

**重叠检测**
- 实时检测任意两个货物是否存在空间重叠（体积交叉）
- 重叠的货物以红色高亮显示
- 显示重叠的体积大小和重叠位置
- 重叠检测采用AABB（轴对齐包围盒）碰撞检测算法

**边界检测**
- 检测货物是否超出集装箱的内部边界
- 超出边界的货物以橙色高亮显示
- 显示超出边界的距离和方向
- 支持分别检测X、Y、Z三个方向的边界

**异常告警**
- 发现碰撞或边界超出时，在3D视图右上角弹出告警提示
- 告警信息包含：异常类型、涉及货物、异常详情
- 支持一键定位到异常货物

### 3.5 历史方案对比模块

#### 3.5.1 方案版本管理

**自动版本保存**
- 每次执行装柜计算后，系统自动保存计算结果为一个方案版本
- 版本自动命名规则：`{任务名称}_{算法名称}_{时间戳}`，如"出口订单A_GA_20260516_143022"
- 用户可自定义方案名称和添加备注说明

**方案状态**
- 草稿：计算完成但未审核
- 已审核：经审核确认的方案
- 已锁定：重要方案锁定，防止误删和误改
- 已归档：装柜完成后的历史方案
- 已废弃：不再使用的方案

**方案操作**
- 查看：查看方案的详细信息和3D可视化
- 对比：选择多个方案进行对比分析
- 复制：基于已有方案创建副本并修改
- 导出：导出方案的装柜清单和示意图
- 删除：删除方案（已锁定和已归档的方案需先解锁/取消归档）

#### 3.5.2 方案对比分析

系统提供直观的多方案对比功能，帮助用户选择最优方案：

**利用率对比**
- 空间利用率柱状图对比：横轴为方案名称，纵轴为利用率百分比
- 重量利用率柱状图对比
- 综合利用率（空间和重量的加权平均）对比

**柜数对比**
- 不同方案使用的集装箱数量对比
- 不同方案的箱型组合对比
- 总成本估算对比（基于各箱型的参考运费）

**约束满足度对比**
- 各方案对各项约束的满足情况以表格形式展示
- 约束项包括：重量约束、重心约束、堆码约束、方向约束、悬空约束、互斥约束、卸货顺序约束
- 满足显示绿色勾号，不满足显示红色叉号和偏离值

**对比视图**
- 支持同时打开两个方案的3D视图进行左右对比
- 同步旋转：两个视图的旋转操作保持同步
- 差异高亮：两个方案中摆放位置不同的货物以特殊颜色标注

#### 3.5.3 历史数据查询

**查询条件**
- 时间范围：按起止日期筛选
- 客户：按客户名称筛选
- 目的地：按目的港口/城市筛选
- 航线：按运输航线筛选
- 集装箱类型：按柜型筛选
- 利用率范围：按空间利用率区间筛选
- 关键词搜索：搜索任务名称、订单号、方案备注等

**查询结果**
- 列表展示：任务名称、柜型、货物种类数、空间利用率、重量利用率、创建时间
- 支持按利用率、时间、柜数等字段排序
- 支持分页显示，每页20/50/100条可配置
- 支持导出查询结果为Excel

#### 3.5.4 方案复用

**相似方案匹配**
- 系统自动分析当前货物清单与历史方案的相似度
- 相似度计算基于：货物种类重叠度、尺寸分布相似度、总体积/总重量接近度
- 相似度超过80%的历史方案自动推荐给用户
- 推荐列表按相似度从高到低排序

**一键复用**
- 用户可选择历史方案一键复用到当前任务
- 复用时系统自动匹配当前货物与历史货物的对应关系
- 货物有差异时（新增/删除/尺寸变化），系统自动标记差异项
- 用户确认后，基于复用方案进行微调计算

**微调重算**
- 在复用方案的基础上，仅对变化部分重新计算
- 微调计算耗时远小于全量计算（通常在5秒内完成）
- 保留原方案中未变化货物的摆放位置不变

### 3.6 报表与导出模块

#### 3.6.1 装柜清单

**货物明细表**

| 序号 | SKU编码 | 货物名称 | 尺寸L×W×H(mm) | 单件重量(kg) | 件数 | 摆放位置X(mm) | 摆放位置Y(mm) | 摆放位置Z(mm) | 摆放方向 | 所在层 |
|------|---------|---------|---------------|-------------|------|-------------|-------------|-------------|---------|--------|
| 1 | SKU001 | 电子元件A | 400×300×250 | 12.5 | 50 | 0 | 0 | 0 | 原始 | 第1层 |
| 2 | SKU002 | 电子元件B | 350×250×200 | 8.3 | 80 | 400 | 0 | 0 | 原始 | 第1层 |

**集装箱汇总表**

| 柜号 | 箱型 | 货物种类数 | 货物总件数 | 总重量(kg) | 总体积(CBM) | 空间利用率 | 重量利用率 | 重心X(mm) | 重心Y(mm) | 重心Z(mm) |
|------|------|-----------|-----------|-----------|-----------|-----------|-----------|----------|----------|----------|
| C1 | 40HQ | 15 | 520 | 18500 | 62.5 | 89.2% | 69.9% | 5980 | 1176 | 1350 |
| C2 | 40GP | 8 | 300 | 22100 | 58.3 | 86.1% | 82.8% | 5990 | 1175 | 1120 |

**导出格式**
- Excel格式（.xlsx）：包含货物明细表和集装箱汇总表两个sheet，支持二次编辑
- PDF格式（.pdf）：格式固定，适合打印和存档

#### 3.6.2 装柜示意图

**2D装柜图**
- 正面图（Front View）：从集装箱正面（开门方向）查看的截面图
- 侧面图（Side View）：从集装箱侧面查看的截面图
- 俯视图（Top View）：从集装箱顶部向下查看的平面图
- 每个2D视图标注货物名称、SKU编码和尺寸

**3D装柜图**
- 可旋转的3D效果图（PNG格式）
- 系统自动选择最佳视角截图（等轴测视角）
- 支持用户自定义视角后截图

**分层装柜图**
- 每层货物的俯视图
- 层间用虚线分隔
- 标注层号和层高范围
- 适合现场操作人员按层装柜

**导出格式**
- PDF格式：包含所有2D视图和分层图，适合打印
- PNG格式：高分辨率图片（300DPI），适合嵌入报告

#### 3.6.3 利用率报告

**统计维度**
- 空间利用率统计：平均利用率、最高利用率、最低利用率、利用率分布直方图
- 重量利用率统计：平均利用率、最高利用率、最低利用率
- 综合利用率 = 空间利用率 × 0.6 + 重量利用率 × 0.4

**趋势分析**
- 按日/周/月维度展示利用率变化趋势
- 折线图展示：横轴为时间，纵轴为利用率
- 同比/环比分析：与上一周期对比，展示改善或退化的幅度

**成本节省分析**
- 基于利用率提升计算节省的运费
- 计算公式：节省运费 = (优化后柜数 - 优化前柜数) × 单柜运费
- 展示累计节省金额趋势图
- 按客户/航线/柜型维度分析成本节省分布

#### 3.6.4 装卸指导书

装卸指导书是面向现场装柜操作员的详细操作指引文件：

**内容结构**
1. **任务概览**：任务名称、订单号、集装箱信息、计划装柜时间
2. **装柜总览**：总货物件数、总重量、预计空间利用率、预计装柜时间
3. **注意事项**：易碎品提醒、重货搬运提醒、特殊约束说明
4. **分层装柜指引**：
   - 第1层（底层）：
     - 操作步骤1：将SKU001（电子元件A）放置在集装箱左后角，原始方向，摆放50件
     - 操作步骤2：将SKU003（五金配件C）放置在SKU001右侧，原始方向，摆放30件
     - ...
   - 第2层：
     - 操作步骤1：在SKU001上方放置SKU002（电子元件B），注意不超过堆码层数限制
     - ...
5. **封柜检查清单**：重量核对、重心确认、绑扎固定确认、拍照存档

**特殊操作提示**
- 易碎品操作：标注"轻拿轻放""不可堆码""使用缓冲材料"
- 重货操作：标注"使用叉车搬运""放置在底层""注意地面承重"
- 冷链操作：标注"快速装柜减少温升""预留通风间距""装柜后立即启动制冷"

**导出格式**
- PDF格式，A4纸张，适合现场打印
- 支持选择打印内容：完整版/简化版（仅分层指引）

### 3.7 系统管理模块

#### 3.7.1 用户权限管理

**角色定义**

| 角色 | 角色描述 | 默认权限 |
|------|---------|---------|
| 系统管理员 | 系统最高权限管理者 | 全部功能权限 + 系统配置 + 用户管理 |
| 物流主管 | 物流部门管理者 | 货物管理 + 装柜计算 + 方案审核 + 报表查看 + 历史方案 |
| 调度员 | 物流调度人员 | 货物管理 + 装柜计算 + 方案查看 + 报表导出 |
| 操作员 | 现场装柜人员 | 方案查看 + 装卸指导书查看 |
| 只读用户 | 外部查看人员（如客户） | 仅查看授权的方案和报表 |

**功能权限控制**
- 按模块控制：货物管理、集装箱管理、装柜计算、3D可视化、方案管理、报表导出、系统管理
- 按操作控制：查看、创建、编辑、删除、导出、审核
- 权限粒度：模块级 > 功能级 > 操作级

**数据权限控制**
- 按组织架构控制：总公司可见全部数据，分公司仅可见本分公司数据，部门仅可见本部门数据
- 按客户控制：某些用户仅能查看特定客户的数据
- 按项目控制：某些用户仅能参与特定项目的装柜任务

**操作审计**
- 记录以下关键操作：用户登录/登出、货物创建/修改/删除、装柜计算执行、方案审核/锁定/删除、系统配置变更、用户权限变更
- 审计日志内容：操作人、操作时间、操作类型、操作对象、操作详情、IP地址
- 日志保留期限：不少于180天
- 支持按时间范围、操作人、操作类型查询审计日志

#### 3.7.2 数据字典管理

系统提供数据字典管理功能，维护各类基础数据：

**集装箱类型维护**
- 新增/编辑/停用集装箱类型
- 维护集装箱的标准参数
- 支持批量导入集装箱参数

**货物类型维护**
- 管理货物分类体系（支持多级分类树）
- 新增/编辑/停用货物类型
- 设置货物类型的默认属性（如纸箱类默认6方向可摆放）

**包装类型维护**
- 管理包装类型列表（纸箱、木箱、托盘等）
- 设置包装类型的默认约束参数

**算法参数模板维护**
- 预设多套算法参数模板（如"快速估算模板""精确优化模板""冷链专用模板"）
- 每套模板包含：推荐算法、计算时间限制、约束参数默认值、优化目标权重
- 用户可在模板基础上微调参数

**国家/地区维护**
- 维护国家/地区列表
- 关联港口信息
- 关联运输路线信息

#### 3.7.3 系统配置

**算法默认参数配置**
- 默认算法选择：FFD/BFD/GA/SA/自动推荐
- 默认计算时间限制：10秒/30秒/60秒
- GA默认参数：种群大小（50-500）、迭代次数（100-10000）、交叉概率（0.6-0.9）、变异概率（0.01-0.1）
- SA默认参数：初始温度、降温系数、终止温度

**约束默认值配置**
- 默认重心偏移允许范围：纵向5%、横向10%
- 默认最小承托面积比例：75%
- 默认最大堆码层数：按包装类型分别设置
- 默认绑扎预留空间：顶部50mm

**通知规则配置**
- 计算完成通知：装柜计算完成后通知相关人员
- 方案审核通知：方案提交审核后通知审核人
- 异常告警通知：重心超标、利用率异常等告警通知
- 通知方式：站内消息、邮件、企业微信/钉钉

**系统主题/语言配置**
- 界面主题：浅色/深色
- 语言：中文/English
- 日期格式：YYYY-MM-DD / DD/MM/YYYY / MM/DD/YYYY
- 数值格式：公制（mm, kg, CBM）/ 英制（inch, lb, CFT）

---

## 4. 非功能需求

### 4.1 性能需求

| 性能指标 | 具体要求 | 测试条件 | 优先级 |
|---------|---------|---------|--------|
| 单柜计算响应时间（FFD/BFD） | ≤ 3秒 | 货物种类≤100，总件数≤1000 | P0 |
| 单柜计算响应时间（GA/SA） | ≤ 60秒（可配置） | 货物种类≤100，总件数≤1000 | P0 |
| 多柜联合计算（≤10柜） | ≤ 120秒 | 货物种类≤200，总件数≤5000 | P1 |
| 多柜联合计算（≤50柜） | ≤ 600秒 | 货物种类≤500，总件数≤20000 | P2 |
| 3D可视化首次渲染 | ≤ 3秒 | 货物件数≤1000 | P0 |
| 3D可视化交互操作 | ≤ 200ms | 旋转/缩放/点击响应 | P0 |
| 货物列表查询 | ≤ 1秒 | 数据量≤10000条 | P1 |
| 报表生成 | ≤ 5秒 | 单次装柜任务报表 | P1 |
| Excel导出 | ≤ 10秒 | 数据量≤5000条 | P1 |
| 并发用户数 | ≥ 50 | 同时在线操作用户 | P0 |
| 系统可用性 | ≥ 99.5% | 月度统计 | P0 |
| 数据库查询响应 | ≤ 500ms | 复杂查询（多表关联） | P1 |

### 4.2 安全需求

**用户认证与授权**
- 支持账号密码登录，密码需满足复杂度要求（至少8位，包含大小写字母和数字）
- 支持集成企业SSO（Single Sign-On）/ LDAP认证
- 支持多因素认证（MFA）：短信验证码、TOTP动态令牌
- 登录失败保护：连续5次失败后锁定账号30分钟
- 会话管理：Token有效期2小时，支持刷新Token，超时自动登出

**数据传输安全**
- 全站HTTPS加密（TLS 1.2+）
- API接口签名验证，防止参数篡改
- 敏感参数（密码、Token）不在URL中传递

**数据存储安全**
- 用户密码使用bcrypt算法加密存储
- 敏感业务数据（客户信息、货物价值）AES-256加密存储
- 数据库定期安全巡检

**安全防护**
- SQL注入防护：使用参数化查询，禁止拼接SQL
- XSS防护：对用户输入进行HTML转义，设置Content-Security-Policy
- CSRF防护：使用Token验证机制
- 文件上传安全：限制文件类型和大小，上传文件病毒扫描
- 接口限流：单个用户每分钟最多调用100次API

**操作审计**
- 关键操作全量记录审计日志
- 审计日志不可篡改（写入后只读）
- 日志保留不少于180天
- 支持审计日志查询和导出

**数据备份与恢复**
- 每日凌晨自动全量备份
- 每小时增量备份
- 备份数据保留30天
- 每月进行一次备份恢复演练
- 备份数据异地存储

### 4.3 可用性需求

**响应式设计**
- 支持PC端（1920×1080及以上分辨率）最佳体验
- 支持Pad端（1024×768及以上分辨率）良好体验
- 关键操作页面在Pad端可正常使用

**浏览器兼容性**
- Google Chrome 80+
- Microsoft Edge 80+
- Mozilla Firefox 75+
- Safari 13+（Mac/iPad）

**操作引导**
- 首次登录显示操作向导，引导用户完成核心功能的使用
- 各功能模块提供帮助文档入口（悬浮帮助按钮）
- 关键操作提供Tooltip提示
- 复杂功能提供示例数据一键填充

**错误处理**
- 友好的错误信息展示：避免展示技术性错误信息（如堆栈跟踪）
- 每条错误信息附带解决建议
- 网络异常时自动重试（最多3次）
- 表单输入实时校验，错误字段即时标红提示

**国际化**
- 支持中文和英文两种语言切换
- 日期、时间、数字格式根据语言自动适配
- 架构支持扩展更多语言（如日语、韩语、西班牙语）

### 4.4 可扩展性需求

**微服务架构**
- 核心模块独立部署：货物管理服务、装柜计算服务、3D可视化服务、报表服务、用户服务
- 各服务通过消息队列（如RabbitMQ/Kafka）进行异步通信
- 支持水平扩展：计算密集型服务（装柜计算服务）可部署多实例，通过负载均衡分配任务

**算法插件化**
- 新算法可通过标准插件接口接入，无需修改核心代码
- 算法插件需实现的标准接口：初始化、执行计算、获取结果、取消计算
- 算法配置通过数据库动态加载，无需重启服务

**API开放平台**
- 提供标准RESTful API，支持第三方系统集成
- API文档自动生成（Swagger/OpenAPI规范）
- 支持API Key认证和OAuth 2.0授权
- 提供API调用配额管理和使用统计

**多租户支持**
- 数据隔离：不同企业的数据完全隔离
- 配置隔离：不同企业可独立配置系统参数
- 资源隔离：计算资源按租户配额分配

### 4.5 兼容性需求

| 维度 | 兼容要求 |
|------|---------|
| 操作系统（服务端） | CentOS 7+、Ubuntu 18.04+、Windows Server 2016+ |
| 操作系统（客户端） | Windows 10+、macOS 10.15+、主流Linux发行版 |
| 移动端适配 | iOS 14+ Safari、Android 10+ Chrome（响应式Web，非原生App） |
| 浏览器 | Chrome 80+、Edge 80+、Firefox 75+、Safari 13+ |
| 文件格式导入 | Excel 2010+（.xlsx/.xls）、CSV（UTF-8） |
| 文件格式导出 | Excel（.xlsx）、PDF、PNG（300DPI） |
| 数据库 | PostgreSQL 12+、MySQL 8.0+ |
| 中间件 | Redis 6.0+、RabbitMQ 3.8+ / Kafka 2.8+ |
| 容器化 | Docker 20.10+、Kubernetes 1.20+ |

---

## 5. 数据模型设计

### 5.1 核心实体关系

本系统的核心数据实体及其关系如下：

```
User（用户）
  ├── 1:N → LoadingTask（装柜任务）：一个用户可创建多个装柜任务
  └── N:N → LoadingTask（参与人）：多个用户可参与同一个任务

CargoGroup（货物分组）
  ├── 1:N → Cargo（货物）：一个分组包含多个货物
  └── N:1 → LoadingTask（装柜任务）：分组属于某个装柜任务

Cargo（货物）
  ├── N:1 → CargoGroup（货物分组）：货物属于某个分组
  ├── N:1 → CargoTemplate（货物模板）：货物可基于模板创建
  └── 1:N → LoadingItem（装柜明细）：一个货物可出现在多个装柜明细中

ContainerType（集装箱类型）
  ├── 1:N → Container（集装箱实例）：一种柜型可对应多个集装箱实例
  └── 内置标准类型 + 用户自定义类型

Container（集装箱实例）
  ├── N:1 → ContainerType（集装箱类型）
  ├── N:1 → LoadingPlan（装柜方案）：集装箱属于某个装柜方案
  └── 1:N → LoadingItem（装柜明细）：一个集装箱包含多个装柜明细

LoadingTask（装柜任务）
  ├── 1:N → LoadingPlan（装柜方案）：一个任务可生成多个方案
  ├── 1:N → CargoGroup（货物分组）：一个任务包含多个货物分组
  ├── N:1 → User（创建人）
  └── N:N → User（参与人）

LoadingPlan（装柜方案）
  ├── N:1 → LoadingTask（装柜任务）
  ├── 1:N → Container（集装箱实例）
  ├── 1:N → LoadingItem（装柜明细）
  └── 包含算法信息、利用率数据、重心数据

LoadingItem（装柜明细）
  ├── N:1 → LoadingPlan（装柜方案）
  ├── N:1 → Container（集装箱实例）
  ├── N:1 → Cargo（货物）
  └── 包含摆放位置(x,y,z)、摆放方向、所在层等信息

SystemConfig（系统配置）
  └── 键值对存储，支持分类管理
```

### 5.2 核心数据表设计

#### 5.2.1 cargo（货物表）

| 字段名 | 数据类型 | 长度 | 必填 | 默认值 | 说明 |
|--------|---------|------|------|--------|------|
| id | BIGINT | - | 是 | 自增 | 主键 |
| task_id | BIGINT | - | 是 | - | 关联装柜任务ID |
| group_id | BIGINT | - | 否 | NULL | 关联货物分组ID |
| sku_code | VARCHAR | 50 | 是 | - | SKU编码，任务内唯一 |
| name | VARCHAR | 100 | 是 | - | 货物名称 |
| description | VARCHAR | 500 | 否 | NULL | 货物描述 |
| barcode | VARCHAR | 50 | 否 | NULL | 条形码 |
| length | DECIMAL | 10,1 | 是 | - | 长度（mm） |
| width | DECIMAL | 10,1 | 是 | - | 宽度（mm） |
| height | DECIMAL | 10,1 | 是 | - | 高度（mm） |
| volume | DECIMAL | 12,4 | 是 | - | 体积（CBM），自动计算 |
| gross_weight | DECIMAL | 10,2 | 是 | - | 毛重（kg） |
| net_weight | DECIMAL | 10,2 | 否 | NULL | 净重（kg） |
| quantity | INT | - | 是 | - | 件数 |
| total_weight | DECIMAL | 12,2 | 是 | - | 总重量（kg），自动计算 |
| total_volume | DECIMAL | 14,4 | 是 | - | 总体积（CBM），自动计算 |
| package_type | VARCHAR | 20 | 是 | 'carton' | 包装类型 |
| orient_x | BOOLEAN | - | 是 | TRUE | 方向1：原始方向是否允许 |
| orient_y | BOOLEAN | - | 是 | TRUE | 方向2：长高互换是否允许 |
| orient_z | BOOLEAN | - | 是 | TRUE | 方向3：长宽互换是否允许 |
| orient_xy | BOOLEAN | - | 是 | TRUE | 方向4：宽高互换是否允许 |
| orient_xz | BOOLEAN | - | 是 | TRUE | 方向5：宽长互换是否允许 |
| orient_yz | BOOLEAN | - | 是 | TRUE | 方向6：高宽互换是否允许 |
| max_stack | INT | - | 是 | 8 | 最大堆码层数 |
| min_support | DECIMAL | 5,2 | 是 | 75.00 | 最小承托面积比例（%） |
| can_bear_weight | BOOLEAN | - | 是 | TRUE | 是否可承重 |
| max_bearing_weight | DECIMAL | 10,2 | 否 | NULL | 最大承重（kg） |
| is_fragile | BOOLEAN | - | 是 | FALSE | 是否易碎 |
| is_compressible | BOOLEAN | - | 是 | FALSE | 是否怕压 |
| is_heavy | BOOLEAN | - | 是 | FALSE | 是否重货 |
| is_liquid | BOOLEAN | - | 是 | FALSE | 是否液体 |
| is_dangerous | BOOLEAN | - | 是 | FALSE | 是否危险品 |
| tolerance | DECIMAL | 5,1 | 是 | 0.0 | 变形公差（mm） |
| value | DECIMAL | 14,2 | 否 | NULL | 货物价值（元） |
| priority | INT | - | 是 | 5 | 装卸优先级（1-10） |
| temp_min | DECIMAL | 5,1 | 否 | NULL | 最低温度要求（℃） |
| temp_max | DECIMAL | 5,1 | 否 | NULL | 最高温度要求（℃） |
| ventilation | INT | - | 否 | 0 | 通风间距（mm） |
| sort_order | INT | - | 是 | 0 | 排序序号 |
| created_by | BIGINT | - | 是 | - | 创建人ID |
| created_at | TIMESTAMP | - | 是 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | 是 | CURRENT_TIMESTAMP | 更新时间 |
| is_deleted | BOOLEAN | - | 是 | FALSE | 软删除标记 |

**索引设计**：
- PRIMARY KEY (id)
- UNIQUE INDEX idx_task_sku (task_id, sku_code)
- INDEX idx_group_id (group_id)
- INDEX idx_created_at (created_at)

#### 5.2.2 container_type（集装箱类型表）

| 字段名 | 数据类型 | 长度 | 必填 | 默认值 | 说明 |
|--------|---------|------|------|--------|------|
| id | BIGINT | - | 是 | 自增 | 主键 |
| code | VARCHAR | 20 | 是 | - | 柜型代码（如20GP、40HQ） |
| name | VARCHAR | 50 | 是 | - | 柜型名称 |
| category | VARCHAR | 20 | 是 | 'dry' | 类别（dry/reefer/open_top/flat_rack/custom） |
| is_standard | BOOLEAN | - | 是 | TRUE | 是否标准柜型 |
| external_length | DECIMAL | 8,1 | 是 | - | 外部长度（mm） |
| external_width | DECIMAL | 8,1 | 是 | - | 外部宽度（mm） |
| external_height | DECIMAL | 8,1 | 是 | - | 外部高度（mm） |
| internal_length | DECIMAL | 8,1 | 是 | - | 内部长度（mm） |
| internal_width | DECIMAL | 8,1 | 是 | - | 内部宽度（mm） |
| internal_height | DECIMAL | 8,1 | 是 | - | 内部高度（mm） |
| door_width | DECIMAL | 8,1 | 否 | NULL | 门框宽度（mm） |
| door_height | DECIMAL | 8,1 | 否 | NULL | 门框高度（mm） |
| internal_volume | DECIMAL | 8,2 | 是 | - | 内部容积（CBM） |
| tare_weight | DECIMAL | 8,1 | 是 | - | 皮重（kg） |
| max_payload | DECIMAL | 8,1 | 是 | - | 最大载重（kg） |
| max_gross | DECIMAL | 8,1 | 是 | - | 最大总重（kg） |
| description | VARCHAR | 200 | 否 | NULL | 适用场景说明 |
| tenant_id | BIGINT | - | 否 | NULL | 租户ID（自定义柜型） |
| created_at | TIMESTAMP | - | 是 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | 是 | CURRENT_TIMESTAMP | 更新时间 |

**索引设计**：
- PRIMARY KEY (id)
- UNIQUE INDEX idx_code (code, tenant_id)
- INDEX idx_category (category)

#### 5.2.3 loading_task（装柜任务表）

| 字段名 | 数据类型 | 长度 | 必填 | 默认值 | 说明 |
|--------|---------|------|------|--------|------|
| id | BIGINT | - | 是 | 自增 | 主键 |
| task_no | VARCHAR | 30 | 是 | - | 任务编号（自动生成） |
| name | VARCHAR | 100 | 是 | - | 任务名称 |
| description | VARCHAR | 500 | 否 | NULL | 任务描述 |
| order_no | VARCHAR | 50 | 否 | NULL | 关联订单号 |
| customer | VARCHAR | 100 | 否 | NULL | 客户名称 |
| destination | VARCHAR | 100 | 否 | NULL | 目的地 |
| scenario | VARCHAR | 20 | 是 | 'sea' | 装柜场景（sea/rail/road/warehouse） |
| status | VARCHAR | 20 | 是 | 'draft' | 状态（draft/calculating/completed/cancelled） |
| total_cargo_types | INT | - | 是 | 0 | 货物种类数 |
| total_cargo_qty | INT | - | 是 | 0 | 货物总件数 |
| total_weight | DECIMAL | 12,2 | 是 | 0 | 货物总重量（kg） |
| total_volume | DECIMAL | 14,4 | 是 | 0 | 货物总体积（CBM） |
| plan_count | INT | - | 是 | 0 | 已生成方案数量 |
| best_utilization | DECIMAL | 5,2 | 是 | 0 | 最优空间利用率（%） |
| created_by | BIGINT | - | 是 | - | 创建人ID |
| assigned_to | BIGINT | - | 否 | NULL | 指定负责人ID |
| planned_date | DATE | - | 否 | NULL | 计划装柜日期 |
| completed_date | DATE | - | 否 | NULL | 实际完成日期 |
| created_at | TIMESTAMP | - | 是 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | 是 | CURRENT_TIMESTAMP | 更新时间 |
| is_deleted | BOOLEAN | - | 是 | FALSE | 软删除标记 |

**索引设计**：
- PRIMARY KEY (id)
- UNIQUE INDEX idx_task_no (task_no)
- INDEX idx_status (status)
- INDEX idx_created_by (created_by)
- INDEX idx_created_at (created_at)

#### 5.2.4 loading_plan（装柜方案表）

| 字段名 | 数据类型 | 长度 | 必填 | 默认值 | 说明 |
|--------|---------|------|------|--------|------|
| id | BIGINT | - | 是 | 自增 | 主键 |
| task_id | BIGINT | - | 是 | - | 关联装柜任务ID |
| plan_no | VARCHAR | 30 | 是 | - | 方案编号 |
| name | VARCHAR | 100 | 是 | - | 方案名称 |
| remark | VARCHAR | 500 | 否 | NULL | 方案备注 |
| algorithm | VARCHAR | 20 | 是 | - | 使用算法（ffd/bfd/ga/sa/hybrid） |
| calc_duration | INT | - | 是 | 0 | 计算耗时（毫秒） |
| container_count | INT | - | 是 | 0 | 集装箱数量 |
| container_types | VARCHAR | 200 | 是 | - | 箱型组合（如"40HQ×2,20GP×1"） |
| total_volume_used | DECIMAL | 14,4 | 是 | 0 | 总使用体积（CBM） |
| total_volume_available | DECIMAL | 14,4 | 是 | 0 | 总可用体积（CBM） |
| space_utilization | DECIMAL | 5,2 | 是 | 0 | 空间利用率（%） |
| total_weight_loaded | DECIMAL | 12,2 | 是 | 0 | 总装载重量（kg） |
| total_weight_available | DECIMAL | 12,2 | 是 | 0 | 总可用载重（kg） |
| weight_utilization | DECIMAL | 5,2 | 是 | 0 | 重量利用率（%） |
| cog_x | DECIMAL | 8,1 | 否 | NULL | 重心X坐标（mm） |
| cog_y | DECIMAL | 8,1 | 否 | NULL | 重心Y坐标（mm） |
| cog_z | DECIMAL | 8,1 | 否 | NULL | 重心Z坐标（mm） |
| cog_deviation_x | DECIMAL | 5,2 | 否 | NULL | 重心X偏移（%） |
| cog_deviation_y | DECIMAL | 5,2 | 否 | NULL | 重心Y偏移（%） |
| cog_deviation_z | DECIMAL | 5,2 | 否 | NULL | 重心Z偏移（%） |
| constraint_violations | INT | - | 是 | 0 | 约束违反数量 |
| estimated_cost | DECIMAL | 12,2 | 否 | NULL | 预估运费成本（元） |
| status | VARCHAR | 20 | 是 | 'draft' | 状态（draft/reviewed/locked/archived/abandoned） |
| is_best | BOOLEAN | - | 是 | FALSE | 是否为最优方案 |
| version | INT | - | 是 | 1 | 方案版本号 |
| created_by | BIGINT | - | 是 | - | 创建人ID |
| reviewed_by | BIGINT | - | 否 | NULL | 审核人ID |
| reviewed_at | TIMESTAMP | - | 否 | NULL | 审核时间 |
| created_at | TIMESTAMP | - | 是 | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | - | 是 | CURRENT_TIMESTAMP | 更新时间 |

**索引设计**：
- PRIMARY KEY (id)
- UNIQUE INDEX idx_plan_no (plan_no)
- INDEX idx_task_id (task_id)
- INDEX idx_algorithm (algorithm)
- INDEX idx_space_utilization (space_utilization)
- INDEX idx_created_at (created_at)

#### 5.2.5 loading_item（装柜明细表）

| 字段名 | 数据类型 | 长度 | 必填 | 默认值 | 说明 |
|--------|---------|------|------|--------|------|
| id | BIGINT | - | 是 | 自增 | 主键 |
| plan_id | BIGINT | - | 是 | - | 关联装柜方案ID |
| container_id | BIGINT | - | 是 | - | 关联集装箱实例ID |
| cargo_id | BIGINT | - | 是 | - | 关联货物ID |
| sequence | INT | - | 是 | - | 装柜顺序 |
| pos_x | DECIMAL | 8,1 | 是 | - | X坐标（mm），距集装箱内壁左端 |
| pos_y | DECIMAL | 8,1 | 是 | - | Y坐标（mm），距集装箱内壁左端 |
| pos_z | DECIMAL | 8,1 | 是 | - | Z坐标（mm），距集装箱底板 |
| orient_length | DECIMAL | 8,1 | 是 | - | 摆放后长度方向尺寸（mm） |
| orient_width | DECIMAL | 8,1 | 是 | - | 摆放后宽度方向尺寸（mm） |
| orient_height | DECIMAL | 8,1 | 是 | - | 摆放后高度方向尺寸（mm） |
| orientation | TINYINT | - | 是 | 1 | 摆放方向编号（1-6） |
| layer | INT | - | 是 | 1 | 所在层号 |
| support_ratio | DECIMAL | 5,2 | 是 | 100.00 | 底部支撑面积比例（%） |
| is_overlap | BOOLEAN | - | 是 | FALSE | 是否存在重叠 |
| is_outbound | BOOLEAN | - | 是 | FALSE | 是否超出边界 |
| created_at | TIMESTAMP | - | 是 | CURRENT_TIMESTAMP | 创建时间 |

**索引设计**：
- PRIMARY KEY (id)
- INDEX idx_plan_id (plan_id)
- INDEX idx_container_id (container_id)
- INDEX idx_cargo_id (cargo_id)
- INDEX idx_sequence (sequence)
- INDEX idx_layer (layer)

---

## 6. 接口设计

### 6.1 外部系统集成接口

#### 6.1.1 WMS集成接口

**出库订单同步接口**

```
接口路径：POST /api/v1/integration/wms/outbound-order
请求方式：异步回调 / 主动拉取
数据格式：JSON
```

请求参数：
```json
{
  "order_no": "WO20260516001",
  "customer": "ABC贸易有限公司",
  "destination": "德国汉堡港",
  "planned_date": "2026-05-20",
  "items": [
    {
      "sku_code": "SKU001",
      "name": "电子元件A",
      "length": 400,
      "width": 300,
      "height": 250,
      "gross_weight": 12.5,
      "quantity": 50,
      "package_type": "carton"
    }
  ]
}
```

**SKU属性同步接口**

```
接口路径：POST /api/v1/integration/wms/sku-sync
触发方式：定时同步（每小时） / 事件触发
```

**装柜方案回传接口**

```
接口路径：POST /api/v1/integration/wms/loading-plan-feedback
触发方式：装柜方案审核通过后自动回传
```

回传数据包含：装柜方案编号、集装箱信息、各SKU的分配数量和摆放位置。

**装载确认接口**

```
接口路径：POST /api/v1/integration/wms/loading-confirmation
触发方式：现场装柜完成后手动确认
```

#### 6.1.2 TMS集成接口

**运输计划同步接口**

```
接口路径：POST /api/v1/integration/tms/transport-plan
数据内容：运输计划编号、预计发车时间、路线信息、车辆/集装箱分配
```

**集装箱分配接口**

```
接口路径：POST /api/v1/integration/tms/container-assign
数据内容：集装箱编号、柜型、司机信息、车牌号
```

**多站配送顺序接口**

```
接口路径：GET /api/v1/integration/tms/delivery-stops/{plan_id}
返回数据：配送站点列表（按顺序）、各站卸货清单
```

#### 6.1.3 ERP集成接口

**销售订单同步接口**

```
接口路径：POST /api/v1/integration/erp/sales-order
数据内容：销售订单号、客户信息、交货日期、物料清单
```

**物料主数据同步接口**

```
接口路径：POST /api/v1/integration/erp/material-master
数据内容：物料编码、名称、基本计量单位、重量、尺寸信息
```

**发货确认回传接口**

```
接口路径：POST /api/v1/integration/erp/shipping-confirmation
数据内容：装柜完成确认、实际装载明细、集装箱封号
```

### 6.2 内部API接口规范

**协议规范**
- 协议：RESTful API over HTTPS
- 数据格式：请求和响应均使用JSON格式
- 字符编码：UTF-8
- 时间格式：ISO 8601（如 "2026-05-16T14:30:22+08:00"）

**认证方式**
- OAuth 2.0 / JWT Token
- 请求头携带：`Authorization: Bearer {access_token}`
- Token有效期：2小时，支持刷新Token

**版本管理**
- URL路径版本化：`/api/v1/...`
- 版本升级策略：新版本发布后，旧版本至少维护6个月

**统一分页参数**
```
GET /api/v1/resources?page=1&pageSize=20&sortBy=created_at&sortOrder=desc
```

- page：页码，从1开始，默认1
- pageSize：每页条数，默认20，最大100
- sortBy：排序字段
- sortOrder：排序方向（asc/desc）

**统一响应格式**

成功响应：
```json
{
  "code": 200,
  "message": "success",
  "data": { ... },
  "timestamp": "2026-05-16T14:30:22+08:00"
}
```

分页响应：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [ ... ],
    "pagination": {
      "page": 1,
      "pageSize": 20,
      "total": 150,
      "totalPages": 8
    }
  },
  "timestamp": "2026-05-16T14:30:22+08:00"
}
```

错误响应：
```json
{
  "code": 40001,
  "message": "货物尺寸不能为空",
  "details": [
    {
      "field": "length",
      "message": "长度不能为空"
    }
  ],
  "timestamp": "2026-05-16T14:30:22+08:00"
}
```

**统一错误码体系**

| 错误码范围 | 类别 | 说明 |
|-----------|------|------|
| 20000-20099 | 认证错误 | Token过期、权限不足等 |
| 20100-20199 | 参数错误 | 必填字段为空、格式不正确等 |
| 20200-20299 | 业务错误 | 数据不存在、状态不允许等 |
| 20300-20399 | 计算错误 | 计算超时、无可行解等 |
| 20400-20499 | 系统错误 | 服务异常、数据库错误等 |

### 6.3 核心API列表

#### 6.3.1 货物管理API

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/cargo | 创建货物 |
| GET | /api/v1/cargo | 获取货物列表（支持分页、搜索） |
| GET | /api/v1/cargo/{id} | 获取货物详情 |
| PUT | /api/v1/cargo/{id} | 更新货物信息 |
| DELETE | /api/v1/cargo/{id} | 删除货物（软删除） |
| POST | /api/v1/cargo/batch-import | 批量导入货物（Excel/CSV） |
| GET | /api/v1/cargo/import-template | 下载导入模板 |
| POST | /api/v1/cargo/template | 保存货物模板 |
| GET | /api/v1/cargo/template | 获取模板列表 |
| GET | /api/v1/cargo/template/{id} | 获取模板详情 |

#### 6.3.2 集装箱管理API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/container-type | 获取集装箱类型列表 |
| GET | /api/v1/container-type/{id} | 获取集装箱类型详情 |
| POST | /api/v1/container-type | 创建自定义集装箱类型 |
| PUT | /api/v1/container-type/{id} | 更新自定义集装箱类型 |
| DELETE | /api/v1/container-type/{id} | 删除自定义集装箱类型 |

#### 6.3.3 装柜任务API

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/loading-task | 创建装柜任务 |
| GET | /api/v1/loading-task | 获取任务列表（支持分页、筛选） |
| GET | /api/v1/loading-task/{id} | 获取任务详情 |
| PUT | /api/v1/loading-task/{id} | 更新任务信息 |
| DELETE | /api/v1/loading-task/{id} | 删除任务（软删除） |
| POST | /api/v1/loading-task/{id}/calculate | 执行装柜计算 |
| GET | /api/v1/loading-task/{id}/calculate-status | 查询计算状态 |
| POST | /api/v1/loading-task/{id}/cancel-calculate | 取消正在进行的计算 |

#### 6.3.4 装柜方案API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/loading-task/{id}/plans | 获取装柜方案列表 |
| GET | /api/v1/loading-plan/{id} | 获取方案详情 |
| PUT | /api/v1/loading-plan/{id} | 更新方案信息（改名、备注） |
| DELETE | /api/v1/loading-plan/{id} | 删除方案 |
| POST | /api/v1/loading-plan/{id}/review | 审核方案 |
| POST | /api/v1/loading-plan/{id}/lock | 锁定方案 |
| POST | /api/v1/loading-plan/{id}/unlock | 解锁方案 |
| GET | /api/v1/loading-plan/{id}/visualization | 获取3D可视化数据 |
| POST | /api/v1/loading-plan/{id}/export | 导出装柜报告 |
| POST | /api/v1/loading-plan/compare | 方案对比 |

#### 6.3.5 报表与统计API

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/report/utilization | 利用率统计报表 |
| GET | /api/v1/report/cost-saving | 成本节省分析报表 |
| GET | /api/v1/report/trend | 利用率趋势数据 |
| GET | /api/v1/report/history | 历史装柜记录查询 |

---

## 7. 算法设计

### 7.1 算法选型策略

#### 7.1.1 FFD（首次适应递减算法）

**算法原理**：

FFD算法是解决装箱问题的经典启发式算法，其核心思想是"先大后小，先到先得"：

1. 将所有待装货物按体积从大到小排序
2. 依次取出每个货物，在三维空间中寻找第一个能容纳它的位置
3. 将货物放置在该位置，更新剩余空间
4. 重复步骤2-3直到所有货物放置完毕或无可用空间

**空间管理策略**：

采用"三维空间分割"策略管理剩余空间。每次放置一个货物后，将原来的剩余空间分割为最多三个子空间：
- 右侧空间：货物右侧到集装箱右壁之间的空间
- 前方空间：货物前方到集装箱前壁之间的空间
- 上方空间：货物上方到集装箱顶壁之间的空间

**伪代码**：

```
function FFD(cargos, container):
    // 步骤1：按体积降序排序
    sorted_cargos = sort(cargos, by=volume, order=descending)

    // 步骤2：初始化空间列表
    spaces = [MaximalSpace(0, 0, 0,
                           container.length,
                           container.width,
                           container.height)]

    placed_items = []

    // 步骤3：依次放置每个货物
    for cargo in sorted_cargos:
        best_space = null
        for space in spaces:
            if can_place(cargo, space, container):
                best_space = space
                break  // 首次适应：选择第一个能容纳的空间

        if best_space != null:
            // 放置货物
            item = place(cargo, best_space)
            placed_items.append(item)

            // 分割空间
            new_spaces = split_space(best_space, cargo)
            remove(spaces, best_space)
            add_all(spaces, new_spaces)

            // 合并相邻空间
            merge_adjacent_spaces(spaces)

    return placed_items
```

**适用场景**：
- 实时快速估算：客户询价时需要快速给出装柜方案
- 货物种类较少（<50种SKU）
- 对计算速度要求高（<3秒）
- 作为GA/SA算法的初始解生成器

**局限性**：
- 容易产生碎片空间，后续大件货物可能无法放入
- 空间利用率不如元启发式算法
- 对约束处理能力有限（难以处理复杂的互斥约束、卸货顺序约束）

#### 7.1.2 BFD（最佳适应递减算法）

**算法原理**：

BFD算法是FFD算法的改进版本，核心区别在于空间选择策略：

1. 将所有待装货物按体积从大到小排序
2. 依次取出每个货物，在所有能容纳它的空间中选择"剩余空间最小"的那个
3. 将货物放置在该位置，更新剩余空间
4. 重复步骤2-3直到所有货物放置完毕或无可用空间

**"最佳适应"的定义**：

对于每个候选空间，计算放置货物后的剩余空间体积：
```
remaining_volume = space.volume - cargo.volume
```
选择 remaining_volume 最小的空间，即"最紧凑地"放入货物。

**伪代码**：

```
function BFD(cargos, container):
    sorted_cargos = sort(cargos, by=volume, order=descending)
    spaces = [MaximalSpace(0, 0, 0,
                           container.length,
                           container.width,
                           container.height)]
    placed_items = []

    for cargo in sorted_cargos:
        best_space = null
        min_remaining = INFINITY

        for space in spaces:
            if can_place(cargo, space, container):
                remaining = space.volume - cargo.volume
                if remaining < min_remaining:
                    min_remaining = remaining
                    best_space = space

        if best_space != null:
            item = place(cargo, best_space)
            placed_items.append(item)
            new_spaces = split_space(best_space, cargo)
            remove(spaces, best_space)
            add_all(spaces, new_spaces)
            merge_adjacent_spaces(spaces)

    return placed_items
```

**适用场景**：
- 实时快速估算，对利用率有一定要求
- 货物尺寸差异较大的场景（BFD能更好地利用小空间）
- 作为混合算法的第一阶段

**与FFD的对比**：
- BFD的空间利用率通常比FFD高2%-5%
- BFD的计算时间略长（需遍历所有候选空间）
- BFD产生的碎片空间更少

#### 7.1.3 GA（遗传算法）

**算法原理**：

遗传算法模拟自然选择和遗传机制，在解空间中进行全局搜索：

1. **编码**：将装箱方案编码为染色体。染色体由两部分组成：
   - 货物排列序列：表示货物的放置顺序
   - 方向序列：表示每个货物的摆放方向
2. **初始化**：随机生成初始种群（或使用FFD/BFD生成部分优质个体）
3. **适应度评估**：对每个个体解码为装箱方案，计算适应度值
4. **选择**：根据适应度值选择优秀个体进入下一代
5. **交叉**：将两个父代个体的部分基因交换，产生子代
6. **变异**：以一定概率随机改变个体的部分基因
7. **终止**：达到最大迭代次数或适应度收敛

**适应度函数设计**：

```
fitness = W_space × space_utilization
        - W_cog × cog_deviation_penalty
        - W_constraint × constraint_violation_penalty
        - W_unplaced × unplaced_volume_ratio
```

其中：
- W_space = 0.6（空间利用率权重）
- W_cog = 0.2（重心偏移惩罚权重）
- W_constraint = 0.1（约束违反惩罚权重）
- W_unplaced = 0.1（未放置货物惩罚权重）
- cog_deviation_penalty = (|deviation_x|/5%)² + (|deviation_y|/10%)²
- constraint_violation_penalty = Σ(violation_severity × violation_count)

**遗传操作**：

- **选择**：锦标赛选择（Tournament Selection），每次随机选取3-5个个体，选择其中适应度最高的
- **交叉**：顺序交叉（Order Crossover, OX）
  - 在父代1中随机选择一个片段
  - 将该片段复制到子代
  - 从父代2中按顺序填充剩余位置
- **变异**：
  - 交换变异：随机选择两个位置，交换其货物
  - 方向变异：随机选择一个货物，随机改变其摆放方向
  - 插入变异：随机选择一个货物，插入到另一个随机位置

**参数配置**：

| 参数 | 默认值 | 范围 | 说明 |
|------|--------|------|------|
| 种群大小 | 100 | 50-500 | 每代的个体数量 |
| 最大迭代次数 | 500 | 100-5000 | 算法运行的最大代数 |
| 交叉概率 | 0.85 | 0.6-0.95 | 发生交叉操作的概率 |
| 变异概率 | 0.05 | 0.01-0.2 | 发生变异操作的概率 |
| 精英保留比例 | 5% | 1%-10% | 直接进入下一代的优秀个体比例 |
| 收敛阈值 | 0.01% | 0.001%-0.1% | 连续N代最优适应度变化小于此值时提前终止 |

#### 7.1.4 SA（模拟退火算法）

**算法原理**：

模拟退火算法模拟固体退火过程，通过接受一定概率的劣解来跳出局部最优：

1. **初始化**：从FFD/BFD生成的初始解出发，设定初始温度T₀
2. **邻域搜索**：在当前解的邻域中随机生成新解
3. **评估**：计算新解的适应度值
4. **接受/拒绝**：
   - 如果新解更优（适应度更高），直接接受
   - 如果新解更差，以概率 P = exp(-ΔE / T) 接受（ΔE为适应度差值，T为当前温度）
5. **降温**：按降温策略降低温度 T = α × T
6. **终止**：温度降至终止温度或达到最大迭代次数

**邻域操作**：

- 随机交换两个货物的放置顺序
- 随机改变一个货物的摆放方向
- 随机将一个货物移动到另一个位置
- 随机交换两个货物的摆放位置

**参数配置**：

| 参数 | 默认值 | 范围 | 说明 |
|------|--------|------|------|
| 初始温度 T₀ | 100 | 10-1000 | 根据初始解适应度自动调整 |
| 降温系数 α | 0.97 | 0.90-0.99 | 每次降温的乘数因子 |
| 终止温度 T_min | 0.01 | 0.001-0.1 | 温度低于此值时停止 |
| 每温度迭代次数 | 50 | 20-200 | 每个温度水平下的迭代次数 |
| 最大总迭代次数 | 10000 | 1000-50000 | 算法的最大总迭代次数 |

### 7.2 混合算法策略

混合算法策略结合了启发式算法的速度优势和元启发式算法的优化能力：

**第一阶段：启发式初始化（FFD/BFD）**
- 使用FFD和BFD分别生成初始解
- 选择适应度更高的初始解作为GA的种子个体
- 同时将初始解的变异版本加入初始种群（增加多样性）

**第二阶段：GA全局优化**
- 以初始种群为基础，执行遗传算法
- 种群中保留10%的FFD/BFD直接生成的个体，90%为随机生成
- 遗传操作中增加"局部搜索"算子：对变异后的个体执行BFD局部优化

**第三阶段：SA精细调优**
- 将GA的最优解作为SA的初始解
- 使用较小的降温系数（α=0.99）进行精细搜索
- 重点关注约束满足度的微调

**终止条件**（满足任一即终止）：
- 达到最大计算时间
- 连续20代最优适应度变化 < 0.01%
- 找到空间利用率 ≥ 95% 的解

### 7.3 约束处理机制

**硬约束处理（必须满足）**

- 重量约束：在适应度函数中对超重方案施加极大惩罚值（-10000）
- 边界约束：在放置货物时直接检查，超出边界的放置操作直接拒绝
- 方向约束：在编码时仅使用允许的方向值
- 互斥约束：在放置货物时检查相邻货物，违反互斥规则的放置直接拒绝

**软约束处理（尽量满足）**

- 重心约束：通过惩罚函数在适应度中体现，违反程度越大惩罚越重
- 堆码优化：通过适应度函数中的"堆码合理性"项引导优化
- 卸货顺序：通过适应度函数中的"顺序违反度"项引导优化

**约束违反惩罚函数**：

```
penalty = Σ(hard_violation × 10000)
        + Σ(soft_violation × weight × severity)

其中：
hard_violation = 硬约束违反次数
soft_violation = 软约束违反程度（归一化到0-1）
weight = 该软约束的权重
severity = 违反严重程度系数
```

### 7.4 算法性能指标

| 算法 | 时间复杂度 | 典型利用率 | 计算时间（100种SKU） | 适用场景 | 约束处理能力 |
|------|-----------|-----------|---------------------|---------|------------|
| FFD | O(n log n) | 70%-82% | < 1秒 | 实时快速估算 | 基础约束 |
| BFD | O(n log n) | 72%-85% | < 2秒 | 实时快速估算 | 基础约束 |
| GA | O(n²)~O(n³) | 85%-95% | 10-60秒 | 离线精确优化 | 全约束 |
| SA | O(n²)~O(n³) | 83%-93% | 10-60秒 | 约束复杂的单柜优化 | 全约束 |
| 混合策略 | O(n²)~O(n³) | 88%-95% | 30-120秒 | 大批量多柜规划 | 全约束 |

---

## 8. 集装箱规格参数

本章提供完整的标准集装箱规格参数，供系统内置使用和用户参考。

### 8.1 干货箱（Dry Container）

干货箱是最常用的集装箱类型，适用于一般干货的运输。

| 参数 | 20GP | 40GP | 40HQ | 45HQ |
|------|------|------|------|------|
| 外部长度 (mm) | 6058 | 12192 | 12192 | 13716 |
| 外部宽度 (mm) | 2438 | 2438 | 2438 | 2438 |
| 外部高度 (mm) | 2591 | 2591 | 2896 | 2896 |
| 内部长度 (mm) | 5898 | 12032 | 12032 | 13556 |
| 内部宽度 (mm) | 2352 | 2352 | 2352 | 2352 |
| 内部高度 (mm) | 2393 | 2393 | 2698 | 2698 |
| 门框宽度 (mm) | 2340 | 2340 | 2340 | 2340 |
| 门框高度 (mm) | 2280 | 2280 | 2585 | 2585 |
| 内部容积 (CBM) | 33.2 | 67.7 | 76.3 | 86.1 |
| 皮重 (kg) | 2230 | 3740 | 3940 | 4820 |
| 最大载重 (kg) | 21770 | 26680 | 26460 | 25580 |
| 最大总重 (kg) | 24000 | 30420 | 30400 | 30400 |
| TEU | 1 | 2 | 2 | 2.25 |

### 8.2 冷藏箱（Reefer Container）

冷藏箱配备制冷机组，用于冷冻或冷藏货物的运输。

| 参数 | 20RF | 40RF | 40RH |
|------|------|------|------|
| 外部长度 (mm) | 6058 | 12192 | 12192 |
| 外部宽度 (mm) | 2438 | 2438 | 2438 |
| 外部高度 (mm) | 2591 | 2591 | 2896 |
| 内部长度 (mm) | 5443 | 11577 | 11577 |
| 内部宽度 (mm) | 2289 | 2289 | 2289 |
| 内部高度 (mm) | 2245 | 2245 | 2548 |
| 门框宽度 (mm) | 2276 | 2276 | 2276 |
| 门框高度 (mm) | 2134 | 2134 | 2438 |
| 内部容积 (CBM) | 27.9 | 58.9 | 67.2 |
| 皮重 (kg) | 3060 | 4760 | 4960 |
| 最大载重 (kg) | 20940 | 25660 | 25460 |
| 最大总重 (kg) | 24000 | 30420 | 30420 |
| 温度范围 | -25°C ~ +25°C | -25°C ~ +25°C | -25°C ~ +25°C |

### 8.3 开顶箱（Open Top Container）

开顶箱顶部可开启，适用于超高货物或需要从顶部吊装的货物。

| 参数 | 20OT | 40OT |
|------|------|------|
| 外部长度 (mm) | 6058 | 12192 |
| 外部宽度 (mm) | 2438 | 2438 |
| 外部高度 (mm) | 2591 | 2591 |
| 内部长度 (mm) | 5898 | 12032 |
| 内部宽度 (mm) | 2352 | 2352 |
| 内部高度 (mm) | 2352 | 2352 |
| 门框宽度 (mm) | 2340 | 2340 |
| 门框高度 (mm) | 2280 | 2280 |
| 内部容积 (CBM) | 32.5 | 66.4 |
| 皮重 (kg) | 2360 | 3820 |
| 最大载重 (kg) | 21640 | 26600 |
| 最大总重 (kg) | 24000 | 30420 |

### 8.4 框架箱（Flat Rack Container）

框架箱没有侧壁和顶壁，仅保留底板和两端端框，适用于超宽、超高、超重货物。

| 参数 | 20FR | 40FR |
|------|------|------|
| 外部长度 (mm) | 6058 | 12192 |
| 外部宽度 (mm) | 2438 | 2438 |
| 外部高度 (mm) | 2591 | 2591 |
| 内部长度 (mm) | 5620 | 11760 |
| 内部宽度 (mm) | 2178 | 2178 |
| 内部高度 (mm) | 2070 | 2070 |
| 内部容积 (CBM) | 25.3 | 53.1 |
| 皮重 (kg) | 2870 | 5300 |
| 最大载重 (kg) | 21130 | 25120 |
| 最大总重 (kg) | 24000 | 30420 |

---

## 9. 验收标准

### 9.1 功能验收标准

| 功能模块 | 验收项 | 验收标准 | 验证方法 | 优先级 |
|---------|--------|---------|---------|--------|
| 货物管理 | 手动录入 | 所有字段正确保存，自动计算字段（体积、总重量）准确 | 录入10种货物，验证数据正确性 | P0 |
| 货物管理 | Excel批量导入 | 支持标准模板导入，数据校验覆盖率100%，失败原因明确 | 导入100条数据（含5条错误数据），验证校验结果 | P0 |
| 货物管理 | 货物模板 | 模板保存、引用、修改功能正常 | 创建5个模板，引用模板创建货物 | P1 |
| 集装箱管理 | 标准柜型 | 内置10种标准柜型，参数准确 | 逐一核对柜型参数 | P0 |
| 集装箱管理 | 自定义箱型 | 支持创建、编辑、删除自定义箱型 | 创建3个自定义箱型并验证 | P1 |
| 装柜计算 | FFD算法 | 3秒内返回结果，空间利用率≥70% | 使用标准测试用例验证 | P0 |
| 装柜计算 | BFD算法 | 3秒内返回结果，空间利用率≥72% | 使用标准测试用例验证 | P0 |
| 装柜计算 | GA算法 | 60秒内返回结果，空间利用率≥85% | 使用标准测试用例验证 | P0 |
| 装柜计算 | SA算法 | 60秒内返回结果，空间利用率≥83% | 使用标准测试用例验证 | P0 |
| 装柜计算 | 多柜联合 | 正确计算最少柜数和最优箱型组合 | 使用200种SKU测试 | P1 |
| 装柜计算 | 约束满足 | 重量、方向、堆码约束100%满足 | 验证10个约束场景 | P0 |
| 3D可视化 | 基础渲染 | 正确渲染装箱效果，颜色编码清晰 | 目视验证5个方案 | P0 |
| 3D可视化 | 交互操作 | 旋转、缩放、点击查看、分层查看功能正常 | 操作验证 | P0 |
| 3D可视化 | 重心显示 | 重心位置和偏移量显示正确 | 对比计算结果 | P1 |
| 3D可视化 | 碰撞检测 | 正确检测并高亮重叠和越界货物 | 验证5个异常场景 | P1 |
| 方案对比 | 利用率对比 | 多方案利用率柱状图正确显示 | 对比3个方案 | P1 |
| 方案对比 | 历史查询 | 按多维度筛选历史方案，结果正确 | 查询验证 | P2 |
| 报表导出 | 装柜清单 | Excel和PDF格式导出，数据准确率100% | 导出并逐条核对 | P0 |
| 报表导出 | 装柜示意图 | 2D/3D/分层图导出，内容完整清晰 | 目视验证 | P1 |
| 报表导出 | 装卸指导书 | PDF格式，内容完整，适合现场打印 | 打印验证 | P1 |
| 系统管理 | 用户权限 | 5种角色权限正确隔离 | 用不同角色账号验证 | P1 |
| 系统管理 | 数据字典 | 各类基础数据维护功能正常 | 增删改查验证 | P2 |

### 9.2 性能验收标准

| 测试项 | 测试条件 | 验收标准 | 测试工具 |
|--------|---------|---------|---------|
| 单柜FFD/BFD计算 | 100种SKU，1000件 | ≤ 3秒 | JMeter / 自定义脚本 |
| 单柜GA/SA计算 | 100种SKU，1000件 | ≤ 60秒 | JMeter / 自定义脚本 |
| 多柜联合计算（10柜） | 200种SKU，5000件 | ≤ 120秒 | JMeter / 自定义脚本 |
| 3D渲染首次加载 | 1000件货物 | ≤ 3秒 | Chrome DevTools |
| 3D交互操作响应 | 旋转/缩放/点击 | ≤ 200ms | Chrome DevTools |
| 50并发用户 | 混合操作场景 | 平均响应时间 ≤ 5秒，错误率 < 1% | JMeter |
| 100并发用户 | 查询类操作 | 平均响应时间 ≤ 3秒 | JMeter |
| 数据库查询 | 复杂多表关联查询 | ≤ 500ms | EXPLAIN ANALYZE |
| Excel导出 | 5000条数据 | ≤ 10秒 | 系统计时 |

### 9.3 安全验收标准

| 测试项 | 验收标准 | 测试方法 |
|--------|---------|---------|
| HTTPS加密 | 所有HTTP请求自动跳转HTTPS，无混合内容 | 浏览器检查 + SSL Labs测试 |
| SQL注入防护 | 所有输入点进行SQL注入测试，无漏洞 | SQLMap扫描 |
| XSS防护 | 所有输入点进行XSS测试，无漏洞 | OWASP ZAP扫描 |
| CSRF防护 | 所有状态变更请求验证CSRF Token | 手动测试 |
| 文件上传安全 | 仅允许指定文件类型，上传文件进行病毒扫描 | 上传恶意文件测试 |
| 接口限流 | 超过限流阈值返回429状态码 | 压力测试 |
| 密码安全 | 密码bcrypt加密存储，不可逆 | 数据库检查 |
| 操作日志 | 关键操作100%记录，日志不可篡改 | 操作后检查审计日志 |
| 数据备份恢复 | 备份数据可成功恢复，数据完整性验证通过 | 模拟故障恢复测试 |

---

## 10. 项目里程碑规划

本项目采用敏捷开发方法，分为三个阶段迭代交付：

### 第一阶段：MVP版本（第1-8周，共8周）

**目标**：交付可用的最小可行产品，包含核心的货物管理、集装箱管理和基础装柜计算功能。

| 周次 | 工作内容 | 交付物 | 里程碑 |
|------|---------|--------|--------|
| 第1周 | 需求评审确认、技术选型、架构设计、开发环境搭建 | 需求确认书、技术方案、架构设计文档 | 需求确认 |
| 第2周 | 数据库设计、API框架搭建、前端框架搭建 | 数据库DDL、API框架代码、前端脚手架 | 基础框架完成 |
| 第3周 | 货物管理模块开发（录入、编辑、删除、列表） | 货物管理功能 | - |
| 第4周 | 集装箱管理模块开发（标准柜型、自定义柜型） | 集装箱管理功能 | - |
| 第5周 | 装柜计算引擎开发（FFD算法、BFD算法、约束求解器） | 装柜计算核心功能 | - |
| 第6周 | 3D可视化基础功能（WebGL渲染、旋转缩放、颜色编码） | 3D可视化基础版 | - |
| 第7周 | 装柜清单导出（Excel/PDF）、装卸指导书生成 | 报表导出功能 | - |
| 第8周 | 集成测试、Bug修复、性能优化、MVP版本发布 | MVP版本（V1.0.0） | **MVP发布** |

**MVP版本功能范围**：
- 货物管理：手动录入、列表管理、基础属性配置
- 集装箱管理：标准柜型库、自定义柜型
- 装柜计算：FFD/BFD算法、基础约束（重量、边界、方向、堆码）
- 3D可视化：基础渲染、旋转缩放、颜色编码
- 报表导出：装柜清单（Excel/PDF）

### 第二阶段：增强版本（第9-14周，共6周）

**目标**：在MVP基础上增加高级算法、批量操作、方案管理和多柜优化功能。

| 周次 | 工作内容 | 交付物 | 里程碑 |
|------|---------|--------|--------|
| 第9周 | GA算法引擎开发（编码、遗传操作、适应度函数） | GA算法功能 | - |
| 第10周 | SA算法引擎开发、混合算法策略实现 | SA算法 + 混合策略 | - |
| 第11周 | Excel批量导入、货物模板库、历史方案管理 | 批量操作 + 方案管理 | - |
| 第12周 | 方案对比分析、相似方案匹配与复用 | 方案对比功能 | - |
| 第13周 | 多柜联合优化、柜型推荐、分柜策略 | 多柜优化功能 | - |
| 第14周 | 系统测试、用户验收测试（UAT）、Bug修复、增强版发布 | 增强版本（V1.1.0） | **增强版发布** |

**增强版本新增功能**：
- 装柜计算：GA/SA算法、混合策略、算法自动推荐
- 货物管理：Excel批量导入、货物模板库
- 方案管理：版本管理、对比分析、历史查询、方案复用
- 多柜优化：多柜联合计算、柜型推荐、混合箱型

### 第三阶段：完善版本（第15-20周，共6周）

**目标**：完成系统集成、专项约束优化、性能调优，交付生产就绪版本。

| 周次 | 工作内容 | 交付物 | 里程碑 |
|------|---------|--------|--------|
| 第15周 | WMS集成接口开发、TMS集成接口开发 | WMS/TMS集成 | - |
| 第16周 | ERP集成接口开发、API开放平台 | ERP集成 + API平台 | - |
| 第17周 | 冷链专项约束（温控分区、通风间距）、危险品隔离约束 | 冷链/危险品约束 | - |
| 第18周 | 多站配送卸货顺序优化（LIFO）、重心可视化增强 | 多站配送优化 | - |
| 第19周 | 全面性能测试、安全测试、压力测试、性能优化 | 测试报告 | - |
| 第20周 | 用户培训、上线部署、试运行、正式版发布 | 正式版本（V2.0.0） | **正式版发布** |

**完善版本新增功能**：
- 系统集成：WMS/TMS/ERP API集成
- 专项约束：冷链约束、危险品约束
- 多站配送：LIFO卸货顺序优化
- 系统管理：完整的用户权限、数据字典、系统配置
- API开放平台：标准RESTful API、Swagger文档

---

## 11. 风险评估

| 序号 | 风险描述 | 概率 | 影响 | 风险等级 | 应对措施 | 责任人 |
|------|---------|------|------|---------|---------|--------|
| 1 | 算法精度不达预期：GA/SA算法在复杂约束场景下利用率低于85% | 中 | 高 | 高 | ① 第2周进行算法POC验证，使用真实业务数据测试 ② 准备备选算法（如蚁群算法、粒子群算法） ③ 与高校算法研究团队建立合作关系 | 技术负责人 |
| 2 | 3D渲染性能不足：货物数量超过1000件时渲染卡顿 | 低 | 中 | 中 | ① 采用WebGL实例化渲染（Instanced Rendering） ② 按需加载：仅渲染视口内的货物 ③ 提供简化渲染模式（低多边形模型） | 前端负责人 |
| 3 | 第三方系统集成复杂：WMS/TMS/ERP系统接口规范不统一 | 中 | 中 | 中 | ① 预留充足联调时间（第15-16周） ② 提前获取第三方系统API文档 ③ 设计适配器模式，降低耦合度 ④ 制定清晰的接口规范和数据映射表 | 后端负责人 |
| 4 | 用户接受度低：现场操作员不习惯使用系统，仍依赖人工经验 | 低 | 高 | 中 | ① MVP阶段邀请核心用户参与试用，收集反馈 ② 装卸指导书设计简洁直观，适合打印 ③ 提供现场培训和操作视频 ④ 设置过渡期，系统方案与人工方案并行 | 产品经理 |
| 5 | 项目进度延期：需求变更或技术难题导致进度延误 | 中 | 中 | 中 | ① 采用敏捷开发，每两周Sprint评估进度 ② 预留20%的缓冲时间 ③ 关键路径上的任务安排经验丰富的开发人员 ④ 及时识别风险并调整计划 | 项目经理 |
| 6 | 数据安全风险：业务数据泄露或丢失 | 低 | 高 | 中 | ① 全站HTTPS + 数据加密存储 ② 完善的备份恢复机制 ③ 定期安全审计和渗透测试 ④ 严格的权限管理和操作审计 | 运维负责人 |
| 7 | 计算资源不足：多用户同时执行GA/SA计算时服务器负载过高 | 中 | 低 | 低 | ① 计算服务支持水平扩展 ② 采用任务队列机制，排队执行计算任务 ③ 设置计算任务优先级 ④ 考虑使用GPU加速 | 架构师 |

---

## 12. 附录

### 附录A：集装箱标准规格参数表

详见第8章，此处不再重复。

### 附录B：算法伪代码

#### B.1 FFD算法伪代码

```
Algorithm: First Fit Decreasing (FFD) for 3D Bin Packing

Input:
    C = list of cargos, each with (length, width, height, weight, quantity)
    B = container with (internal_length, internal_width, internal_height, max_payload)

Output:
    P = list of placed items, each with (cargo_id, x, y, z, orientation, layer)

Steps:
    1. Expand cargo list by quantity: create individual items for each cargo
    2. Sort items by volume in descending order
    3. Initialize space list S = {(0, 0, 0, B.L, B.W, B.H)}
    4. Initialize placed list P = []
    5. Initialize total_weight = 0

    6. FOR each item i in sorted items:
        6.1 IF total_weight + i.weight > B.max_payload:
            SKIP item i (weight constraint violated)
            CONTINUE to next item

        6.2 FOR each allowed orientation o of item i:
            6.2.1 Calculate oriented dimensions: (l, w, h) = orient(i, o)
            6.2.2 FOR each space s in S:
                6.2.2.1 IF l <= s.length AND w <= s.width AND h <= s.height:
                    6.2.2.1.1 Check support constraint
                    6.2.2.1.2 IF support OK:
                        PLACE item i at (s.x, s.y, s.z) with orientation o
                        ADD to P
                        total_weight += i.weight
                        SPLIT space s into sub-spaces
                        MERGE adjacent sub-spaces in S
                        BREAK (move to next item)

    7. RETURN P
```

#### B.2 GA算法伪代码

```
Algorithm: Genetic Algorithm for 3D Bin Packing

Input:
    C = list of cargos
    B = container parameters
    params = {population_size, max_generations, crossover_rate, mutation_rate}

Output:
    best_solution = best placement found

Steps:
    1. // Encoding: chromosome = (permutation of cargo IDs, orientation for each cargo)

    2. // Initialize population
    population = []
    2.1 Generate initial solution using FFD: sol_ffd
    2.2 Generate initial solution using BFD: sol_bfd
    2.3 ADD encode(sol_ffd) to population
    2.4 ADD encode(sol_bfd) to population
    2.5 FOR i = 3 TO population_size:
        chromosome = random_permutation(C.ids) + random_orientations(C.ids)
        ADD chromosome to population

    3. best_fitness = -INFINITY
    best_solution = null

    4. FOR generation = 1 TO max_generations:
        4.1 // Evaluate fitness
        fitnesses = []
        FOR each chromosome in population:
            solution = decode(chromosome, C, B)
            f = evaluate_fitness(solution, B)
            fitnesses.append(f)
            IF f > best_fitness:
                best_fitness = f
                best_solution = solution

        4.2 // Check convergence
        IF best_fitness_change < convergence_threshold for 20 generations:
            BREAK

        4.3 // Selection (Tournament)
        new_population = []
        elite_count = population_size * 0.05
        new_population += top individuals by fitness (elitism)

        4.4 // Crossover and Mutation
        WHILE len(new_population) < population_size:
            parent1 = tournament_select(population, fitnesses, k=3)
            parent2 = tournament_select(population, fitnesses, k=3)

            IF random() < crossover_rate:
                child1, child2 = order_crossover(parent1, parent2)
            ELSE:
                child1, child2 = parent1, parent2

            IF random() < mutation_rate:
                child1 = swap_mutate(child1)
            IF random() < mutation_rate:
                child2 = swap_mutate(child2)

            new_population.append(child1)
            new_population.append(child2)

        4.5 population = new_population

    5. RETURN best_solution
```

#### B.3 适应度评估函数伪代码

```
Function: evaluate_fitness(solution, container)

Input:
    solution = list of placed items with positions and orientations
    container = container parameters

Output:
    fitness = float value (higher is better)

Steps:
    1. Calculate space utilization:
        used_volume = sum(item.length * item.width * item.height for item in solution)
        available_volume = container.internal_length * container.internal_width * container.internal_height
        space_util = used_volume / available_volume

    2. Calculate weight utilization:
        total_weight = sum(item.weight for item in solution)
        weight_util = total_weight / container.max_payload

    3. Calculate center of gravity:
        cog_x = sum(item.weight * (item.x + item.length/2)) / total_weight
        cog_y = sum(item.weight * (item.y + item.width/2)) / total_weight
        cog_z = sum(item.weight * (item.z + item.height/2)) / total_weight

    4. Calculate COG deviation penalty:
        ideal_x = container.internal_length / 2
        ideal_y = container.internal_width / 2
        max_z = container.internal_height * 0.6
        dev_x = abs(cog_x - ideal_x) / (container.internal_length * 0.05)
        dev_y = abs(cog_y - ideal_y) / (container.internal_width * 0.10)
        dev_z = max(0, (cog_z - max_z)) / max_z
        cog_penalty = dev_x^2 + dev_y^2 + dev_z^2

    5. Calculate constraint violation penalty:
        violations = 0
        FOR each item in solution:
            IF item.stack_count > item.max_stack: violations += 1
            IF item.support_ratio < item.min_support: violations += 1
            IF item.orientation not in item.allowed_orientations: violations += 10
        constraint_penalty = violations * 1000

    6. Calculate unplaced penalty:
        unplaced_volume = total_cargo_volume - used_volume
        unplaced_penalty = (unplaced_volume / available_volume) * 10

    7. Combine fitness:
        fitness = 0.60 * space_util
                - 0.20 * cog_penalty
                - 0.10 * constraint_penalty
                - 0.10 * unplaced_penalty

    8. RETURN fitness
```

### 附录C：API接口详细定义

#### C.1 创建装柜任务

```
POST /api/v1/loading-task
Content-Type: application/json
Authorization: Bearer {token}

Request Body:
{
    "name": "2026年5月出口德国订单",
    "description": "客户ABC公司5月出口订单，目的地汉堡港",
    "order_no": "SO20260516001",
    "customer": "ABC贸易有限公司",
    "destination": "德国汉堡港",
    "scenario": "sea",
    "planned_date": "2026-05-20"
}

Response (201 Created):
{
    "code": 201,
    "message": "success",
    "data": {
        "id": 1001,
        "task_no": "LT20260516001",
        "name": "2026年5月出口德国订单",
        "status": "draft",
        "created_at": "2026-05-16T14:30:22+08:00"
    }
}
```

#### C.2 执行装柜计算

```
POST /api/v1/loading-task/{id}/calculate
Content-Type: application/json
Authorization: Bearer {token}

Request Body:
{
    "algorithm": "auto",
    "max_calc_time": 60,
    "container_type_id": 3,
    "container_count": 1,
    "constraints": {
        "max_payload": 26460,
        "cog_x_max_deviation": 5,
        "cog_y_max_deviation": 10,
        "cog_z_max_ratio": 60,
        "min_support_ratio": 75
    },
    "optimization": {
        "space_weight": 0.6,
        "cog_weight": 0.2,
        "efficiency_weight": 0.1,
        "safety_weight": 0.1
    },
    "multi_algorithm": true
}

Response (202 Accepted):
{
    "code": 202,
    "message": "计算任务已提交",
    "data": {
        "task_id": 1001,
        "calculation_id": "CALC20260516143022",
        "status": "calculating",
        "estimated_time": 45
    }
}
```

#### C.3 获取3D可视化数据

```
GET /api/v1/loading-plan/{id}/visualization
Authorization: Bearer {token}

Response (200 OK):
{
    "code": 200,
    "message": "success",
    "data": {
        "plan_id": 2001,
        "container": {
            "type": "40HQ",
            "internal_length": 12032,
            "internal_width": 2352,
            "internal_height": 2698
        },
        "items": [
            {
                "cargo_id": 1,
                "sku_code": "SKU001",
                "name": "电子元件A",
                "color": "#4A90D9",
                "position": {
                    "x": 0, "y": 0, "z": 0
                },
                "dimensions": {
                    "length": 400, "width": 300, "height": 250
                },
                "orientation": 1,
                "layer": 1,
                "quantity": 50
            }
        ],
        "center_of_gravity": {
            "x": 5980, "y": 1176, "z": 1350
        },
        "statistics": {
            "space_utilization": 89.2,
            "weight_utilization": 69.9,
            "total_items": 520,
            "total_layers": 8
        }
    }
}
```

### 附录D：数据字典

#### D.1 包装类型（package_type）

| 编码 | 名称 | 说明 | 默认堆码层数 | 默认方向限制 |
|------|------|------|------------|------------|
| carton | 纸箱 | 瓦楞纸箱包装 | 8 | 全部方向 |
| wooden | 木箱 | 木质箱体包装 | 5 | 仅原始方向 |
| pallet | 托盘 | 放置在托盘上的货物 | 3 | 仅原始方向 |
| bulk | 散装 | 无固定包装 | 1 | 仅原始方向 |
| unpacked | 裸装 | 无包装的大件货物 | 1 | 仅原始方向 |
| drum | 桶装 | 圆柱形桶装货物 | 4 | 仅直立方向 |
| pipe | 管材 | 长条形管状货物 | 10 | 仅水平方向 |

#### D.2 装柜场景（scenario）

| 编码 | 名称 | 说明 |
|------|------|------|
| sea | 海运出口 | 港口装柜，海运运输 |
| rail | 铁路运输 | 火车站台装柜，铁路运输 |
| road | 公路运输 | 仓库/工厂装柜，公路运输 |
| warehouse | 仓储出库 | 仓库内装柜出库 |

#### D.3 算法类型（algorithm）

| 编码 | 名称 | 说明 | 典型计算时间 |
|------|------|------|------------|
| ffd | 首次适应递减 | 启发式算法，速度快 | < 3秒 |
| bfd | 最佳适应递减 | 启发式算法，利用率较高 | < 3秒 |
| ga | 遗传算法 | 元启发式算法，全局优化 | 10-60秒 |
| sa | 模拟退火 | 元启发式算法，跳出局部最优 | 10-60秒 |
| hybrid | 混合策略 | FFD初始化 + GA优化 | 30-120秒 |
| auto | 自动推荐 | 系统根据场景自动选择 | 视推荐算法而定 |

#### D.4 方案状态（plan_status）

| 编码 | 名称 | 说明 |
|------|------|------|
| draft | 草稿 | 计算完成，待审核 |
| reviewed | 已审核 | 审核通过 |
| locked | 已锁定 | 锁定防止修改 |
| archived | 已归档 | 装柜完成，方案归档 |
| abandoned | 已废弃 | 不再使用 |

#### D.5 任务状态（task_status）

| 编码 | 名称 | 说明 |
|------|------|------|
| draft | 草稿 | 任务创建中 |
| ready | 就绪 | 货物和集装箱配置完成 |
| calculating | 计算中 | 正在执行装柜计算 |
| completed | 已完成 | 已生成装柜方案 |
| cancelled | 已取消 | 任务取消 |

---

> **文档变更记录**

| 版本 | 日期 | 变更人 | 变更内容 |
|------|------|--------|---------|
| V1.0 | 2026-05-16 | 产品团队 | 初始版本，完成全部章节编写 |

---

*本文档为物流集装箱装柜测算系统的产品需求文档，如有疑问请联系产品团队。*
